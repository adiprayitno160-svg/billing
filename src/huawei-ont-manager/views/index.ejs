<!DOCTYPE html>
<html lang="id">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Huawei ONT Manager - Premium</title>
    <link rel="stylesheet" href="/css/style.css">
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600;700&family=Fira+Code&display=swap"
        rel="stylesheet">
    <script src="/socket.io/socket.io.js"></script>
</head>

<body>
    <div class="container animate-fade">
        <header>
            <div>
                <h1>HUAWEI ONT MANAGER</h1>
                <p style="color: var(--text-dim); font-size: 0.8rem;">MA5680T Series Automated Provisioning</p>
            </div>
            <div id="connection-status" class="badge badge-new"
                style="background: rgba(56, 189, 248, 0.2); color: var(--primary);">
                OLT: CONNECTED
            </div>
        </header>

        <div class="grid">
            <div class="main-content">
                <div class="card">
                    <div
                        style="display: flex; justify-content: space-between; align-items: center; margin-bottom: 1.5rem;">
                        <h2 style="font-size: 1.1rem;">ONT Autofind List</h2>
                        <button class="btn btn-primary" onclick="scanOnt()">Refresh Scan</button>
                    </div>
                    <table>
                        <thead>
                            <tr>
                                <th>Interface</th>
                                <th>Serial Number</th>
                                <th>Type</th>
                                <th>Action</th>
                            </tr>
                        </thead>
                        <tbody id="ont-list">
                            <!-- Data will be populated here -->
                            <tr>
                                <td colspan="4" style="text-align: center; color: var(--text-dim);">Scanning for new
                                    ONT...</td>
                            </tr>
                        </tbody>
                    </table>
                </div>
            </div>

            <div class="sidebar">
                <div class="card">
                    <h2 style="font-size: 1.1rem; margin-bottom: 1rem;">Live CLI Console</h2>
                    <div id="terminal" class="terminal">
                        <div class="line">Initializing session...</div>
                    </div>
                </div>

                <div class="card">
                    <h2 style="font-size: 1.1rem; margin-bottom: 1rem;">OLT Info</h2>
                    <div style="font-size: 0.875rem; color: var(--text-dim);">
                        <div style="margin-bottom: 0.5rem;">IP: <span
                                style="color: var(--text-main);">192.168.1.100</span></div>
                        <div style="margin-bottom: 0.5rem;">Uptime: <span style="color: var(--text-main);">12d 4h
                                22m</span></div>
                        <div>CPU: <span style="color: var(--text-main);">12%</span></div>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <!-- Modal Approval (Hidden by default) -->
    <div id="approve-modal"
        style="display: none; position: fixed; inset: 0; background: rgba(0,0,0,0.8); backdrop-filter: blur(4px); z-index: 50; align-items: center; justify-content: center;">
        <div class="card" style="width: 400px; margin-bottom: 0;">
            <h2 style="margin-bottom: 1.5rem;">Approve ONT</h2>
            <form id="approve-form">
                <input type="hidden" id="modal-sn">
                <input type="hidden" id="modal-interface">

                <div style="margin-bottom: 1rem;">
                    <label
                        style="display: block; font-size: 0.75rem; color: var(--text-dim); margin-bottom: 0.5rem;">Customer
                        Name</label>
                    <input type="text" id="customer-name"
                        style="width: 100%; background: rgba(0,0,0,0.2); border: 1px solid rgba(255,255,255,0.1); padding: 0.75rem; border-radius: 0.5rem; color: white;">
                </div>

                <div style="margin-bottom: 1rem;">
                    <label
                        style="display: block; font-size: 0.75rem; color: var(--text-dim); margin-bottom: 0.5rem;">VLAN
                        ID</label>
                    <input type="number" id="vlan-id" value="100"
                        style="width: 100%; background: rgba(0,0,0,0.2); border: 1px solid rgba(255,255,255,0.1); padding: 0.75rem; border-radius: 0.5rem; color: white;">
                </div>

                <div style="display: flex; gap: 1rem; margin-top: 2rem;">
                    <button type="button" class="btn" style="flex: 1; background: rgba(255,255,255,0.1); color: white;"
                        onclick="closeModal()">Cancel</button>
                    <button type="submit" class="btn btn-primary" style="flex: 1;">Provision Now</button>
                </div>
            </form>
        </div>
    </div>

    <script src="/js/app.js"></script>
</body>

</html>