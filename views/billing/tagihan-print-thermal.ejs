<!DOCTYPE html>
<html lang="id">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Print Thermal - <%= invoice.invoice_number %></title>
    <style>
        @page {
            size: 58mm auto;
            margin: 2mm 3mm;
        }
        
        @media print {
            body {
                -webkit-print-color-adjust: exact;
                print-color-adjust: exact;
                width: 58mm;
            }
            .no-print {
                display: none !important;
            }
        }
        
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }
        
        body {
            font-family: 'Courier New', monospace;
            font-size: 8pt;
            line-height: 1.3;
            color: #000;
            width: 58mm;
            max-width: 58mm;
            margin: 0 auto;
            background: white;
        }
        
        .container {
            padding: 2mm;
        }
        
        .header {
            text-align: center;
            margin-bottom: 5px;
            border-bottom: 1px dashed #000;
            padding-bottom: 5px;
        }
        
        .company-name {
            font-size: 10pt;
            font-weight: bold;
            margin-bottom: 2px;
            text-transform: uppercase;
        }
        
        .company-details {
            font-size: 7pt;
            line-height: 1.3;
        }
        
        .invoice-title {
            font-size: 11pt;
            font-weight: bold;
            text-align: center;
            margin: 5px 0 3px 0;
            letter-spacing: 1px;
            text-transform: uppercase;
        }
        
        .invoice-number {
            text-align: center;
            font-size: 8pt;
            font-weight: bold;
            margin-bottom: 5px;
        }
        
        .section {
            margin-bottom: 5px;
        }
        
        .section-title {
            font-size: 8pt;
            font-weight: bold;
            border-bottom: 1px solid #000;
            margin-bottom: 3px;
            padding-bottom: 2px;
            text-transform: uppercase;
        }
        
        .row {
            display: flex;
            justify-content: space-between;
            margin-bottom: 2px;
            font-size: 7pt;
        }
        
        .label {
            font-weight: normal;
            flex-shrink: 0;
        }
        
        .value {
            font-weight: bold;
            text-align: right;
            word-break: break-word;
            max-width: 60%;
        }
        
        .items-table {
            width: 100%;
            border-collapse: collapse;
            margin-bottom: 5px;
            font-size: 7pt;
        }
        
        .items-table thead {
            border-bottom: 1px dashed #000;
        }
        
        .items-table th {
            font-weight: bold;
            padding: 2px 0;
            text-align: left;
            font-size: 7pt;
        }
        
        .items-table td {
            padding: 3px 0;
            vertical-align: top;
            font-size: 7pt;
        }
        
        .items-table .desc {
            width: 55%;
        }
        
        .items-table .price {
            width: 45%;
            text-align: right;
            font-weight: bold;
        }
        
        .separator {
            border-top: 1px dashed #000;
            margin: 5px 0;
        }
        
        .total-row {
            display: flex;
            justify-content: space-between;
            font-size: 8pt;
            margin-bottom: 2px;
        }
        
        .total-label {
            font-weight: normal;
        }
        
        .total-value {
            font-weight: bold;
        }
        
        .grand-total {
            border-top: 2px solid #000;
            padding-top: 5px;
            margin: 5px 0;
            font-size: 9pt;
            font-weight: bold;
            background: #fff;
        }
        
        .payment-info {
            text-align: center;
            font-size: 6.5pt;
            margin: 5px 0;
            padding: 5px;
            border: 1px dashed #000;
            line-height: 1.4;
        }
        
        .footer {
            text-align: center;
            font-size: 6pt;
            margin-top: 5px;
            border-top: 1px dashed #000;
            padding-top: 5px;
            line-height: 1.4;
        }
        
        .status-badge {
            display: inline-block;
            padding: 3px 8px;
            border: 2px solid #000;
            font-size: 9pt;
            font-weight: bold;
            text-align: center;
            margin: 5px 0;
        }
        
        .bold {
            font-weight: bold;
        }
        
        .center {
            text-align: center;
        }
        
        .right {
            text-align: right;
        }
        
        .barcode {
            text-align: center;
            font-size: 9pt;
            letter-spacing: 2px;
            margin: 5px 0;
            font-family: 'Courier New', monospace;
            font-weight: bold;
        }
        
        @media screen {
            body {
                width: auto;
                max-width: 100%;
                padding: 20px;
            }
        }
    </style>
</head>
<body>
    <!-- Print Button -->
    <div class="no-print" style="position: fixed; top: 10px; right: 10px; z-index: 1000; background: white; padding: 10px; border-radius: 8px; box-shadow: 0 2px 8px rgba(0,0,0,0.2);">
        <button onclick="window.print()" style="background: #007bff; color: white; padding: 8px 16px; border: none; border-radius: 4px; cursor: pointer; font-size: 12px; margin-right: 5px;">
            <i class="fas fa-print"></i> Print Thermal 58mm
        </button>
        <button onclick="window.close()" style="background: #6c757d; color: white; padding: 8px 16px; border: none; border-radius: 4px; cursor: pointer; font-size: 12px;">
            <i class="fas fa-times"></i> Tutup
        </button>
    </div>

    <div class="container">
        <!-- Header -->
        <div class="header">
            <div class="company-name">PT FIBER OPTIK NUSANTARA</div>
            <div class="company-details">
                Jl. Internet No. 123<br>
                Kota Digital<br>
                Telp: (021) 1234-5678
            </div>
        </div>

        <!-- Invoice Title -->
        <div class="invoice-title">TAGIHAN PEMBAYARAN</div>
        <div class="invoice-number"><%= invoice.invoice_number %></div>
        
        <!-- Barcode/Code -->
        <div class="barcode">*<%= invoice.invoice_number %>*</div>

        <div class="separator"></div>

        <!-- Invoice Info -->
        <div class="section">
            <div class="row">
                <span class="label">Tanggal</span>
                <span class="value"><%= new Date(invoice.created_at).toLocaleDateString('id-ID', { day: '2-digit', month: '2-digit', year: 'numeric' }) %></span>
            </div>
            <div class="row">
                <span class="label">Periode</span>
                <span class="value">
                <% 
                // Format periode dari "2024-01" menjadi "Januari 2024"
                const periodParts = invoice.period.split('-');
                const year = periodParts[0];
                const monthNum = parseInt(periodParts[1]);
                const monthNames = ['Januari', 'Februari', 'Maret', 'April', 'Mei', 'Juni', 'Juli', 'Agustus', 'September', 'Oktober', 'November', 'Desember'];
                const monthName = monthNames[monthNum - 1] || invoice.period;
                %>
                <%= monthName %> <%= year %>
                </span>
            </div>
            <div class="row">
                <span class="label">Jatuh Tempo</span>
                <span class="value bold"><%= new Date(invoice.due_date).toLocaleDateString('id-ID', { day: '2-digit', month: '2-digit', year: 'numeric' }) %></span>
            </div>
        </div>

        <div class="separator"></div>

        <!-- Customer Info -->
        <div class="section">
            <div class="section-title">PELANGGAN</div>
            <div class="row">
                <span class="label">Nama</span>
                <span class="value"><%= invoice.customer_name %></span>
            </div>
            <% if (invoice.customer_code) { %>
                <div class="row">
                    <span class="label">Kode</span>
                    <span class="value"><%= invoice.customer_code %></span>
                </div>
            <% } %>
            <% if (invoice.customer_address) { %>
                <div class="row">
                    <span class="label">Alamat</span>
                    <span class="value"><%= invoice.customer_address %></span>
                </div>
            <% } %>
        </div>

        <div class="separator"></div>

        <!-- Items -->
        <div class="section">
            <div class="section-title">RINCIAN</div>
            <table class="items-table">
                <thead>
                    <tr>
                        <th class="desc">Item</th>
                        <th class="price">Harga</th>
                    </tr>
                </thead>
                <tbody>
                    <% if (items && items.length > 0) { %>
                        <% items.forEach(item => { %>
                            <tr>
                                <td class="desc">
                                    <%= item.description %><br>
                                    <span style="font-size: 7pt; color: #666;"><%= item.quantity || 1 %> x Rp <%= new Intl.NumberFormat('id-ID').format(item.unit_price) %></span>
                                </td>
                                <td class="price bold">
                                    <%= new Intl.NumberFormat('id-ID').format(item.total_price) %>
                                </td>
                            </tr>
                        <% }); %>
                    <% } %>
                    <% if (invoice.discount_amount && invoice.discount_amount > 0) { %>
                        <tr style="color: #d9534f;">
                            <td class="desc">
                                Diskon
                                <% if (invoice.discount_reason) { %>
                                <br><span style="font-size: 6.5pt;">(<%= invoice.discount_reason %>)</span>
                                <% } %>
                                <% if (invoice.sla_type) { %>
                                <br><span style="font-size: 6.5pt;">SLA: <%= invoice.sla_type %></span>
                                <% } %>
                            </td>
                            <td class="price bold">
                                - <%= new Intl.NumberFormat('id-ID').format(invoice.discount_amount) %>
                            </td>
                        </tr>
                    <% } %>
                </tbody>
            </table>
        </div>

        <div class="separator"></div>

        <!-- Totals -->
        <div class="section">
            <div class="total-row">
                <span class="total-label">Subtotal</span>
                <span class="total-value">Rp <%= new Intl.NumberFormat('id-ID').format(invoice.subtotal || invoice.total_amount) %></span>
            </div>
            <% if (invoice.discount_amount && invoice.discount_amount > 0) { %>
                <div class="total-row" style="color: #d9534f;">
                    <span class="total-label">
                        Diskon
                        <% if (invoice.discount_reason) { %>
                        <br><span style="font-size: 6pt; font-weight: normal;">(<%= invoice.discount_reason %>)</span>
                        <% } %>
                        <% if (invoice.sla_type) { %>
                        <br><span style="font-size: 6pt; font-weight: normal;">SLA: <%= invoice.sla_type %></span>
                        <% } %>
                    </span>
                    <span class="total-value">- Rp <%= new Intl.NumberFormat('id-ID').format(invoice.discount_amount) %></span>
                </div>
            <% } %>
            
            <div class="grand-total">
                <div class="total-row" style="font-size: 11pt;">
                    <span class="total-label">TOTAL</span>
                    <span class="total-value">Rp <%= new Intl.NumberFormat('id-ID').format(invoice.total_amount) %></span>
                </div>
            </div>
            
            <% if (invoice.paid_amount > 0) { %>
                <div class="total-row">
                    <span class="total-label">Terbayar</span>
                    <span class="total-value" style="color: #2e7d32;">Rp <%= new Intl.NumberFormat('id-ID').format(invoice.paid_amount) %></span>
                </div>
                <div class="total-row">
                    <span class="total-label bold">Sisa</span>
                    <span class="total-value" style="color: #d32f2f;">Rp <%= new Intl.NumberFormat('id-ID').format(invoice.remaining_amount || (invoice.total_amount - invoice.paid_amount)) %></span>
                </div>
            <% } %>
        </div>

        <!-- Payment Info -->
        <div class="payment-info">
            <div class="bold" style="margin-bottom: 1mm;">INFORMASI PEMBAYARAN</div>
            <div style="font-size: 6.5pt;">
                Harap bayar sebelum jatuh tempo<br>
                Konfirmasi via WhatsApp atau email<br>
                <% if (invoice.notes) { %>
                    <div style="margin-top: 1mm; font-style: italic;"><%= invoice.notes %></div>
                <% } %>
            </div>
        </div>

        <!-- Footer -->
        <div class="footer">
            Terima kasih atas kepercayaan Anda<br>
            <%= new Date().toLocaleString('id-ID', { day: '2-digit', month: '2-digit', year: 'numeric', hour: '2-digit', minute: '2-digit' }) %>
        </div>
    </div>

    <script>
        // Auto print on load (optional)
        // window.onload = function() { window.print(); }
    </script>
</body>
</html>
