<!DOCTYPE html>
<html lang="id">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Invoice #<%= invoice.invoice_number %>
    </title>
    <style>
        /* RESET & BASE */
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background: #e5e7eb;
            color: #1f2937;
        }

        /* UTILITIES */
        .no-print {
            background: #374151;
            padding: 10px;
            color: white;
            display: flex;
            justify-content: center;
            gap: 10px;
            position: sticky;
            top: 0;
            z-index: 1000;
            box-shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.1);
            flex-wrap: wrap;
        }

        button {
            cursor: pointer;
            padding: 8px 16px;
            border-radius: 4px;
            border: none;
            font-weight: 600;
            display: flex;
            align-items: center;
            gap: 5px;
            transition: all 0.2s;
            font-size: 13px;
        }

        .btn-a4 {
            background: #2563eb;
            color: white;
        }

        .btn-thermal {
            background: #059669;
            color: white;
        }

        .btn-bluetooth {
            background: #7c3aed;
            color: white;
        }

        .btn-close {
            background: #ef4444;
            color: white;
        }

        /* ==== A4 MODE STYLES ==== */
        .page-a4 {
            background: white;
            width: 210mm;
            min-height: 297mm;
            margin: 20px auto;
            padding: 20mm;
            box-shadow: 0 10px 15px -3px rgba(0, 0, 0, 0.1);
            display: block;
        }

        /* ... (A4 styles kept minimal for brevity, focusing on thermal fix) ... */
        .a4-header {
            display: flex;
            justify-content: space-between;
            border-bottom: 2px solid #2563eb;
            padding-bottom: 20px;
            margin-bottom: 30px;
        }

        .a4-title {
            font-size: 24pt;
            font-weight: 800;
            color: #2563eb;
            text-transform: uppercase;
        }

        .a4-table {
            width: 100%;
            border-collapse: collapse;
            margin: 20px 0;
        }

        .a4-table th {
            background: #f3f4f6;
            padding: 12px;
            text-align: left;
            border-bottom: 2px solid #e5e7eb;
            font-size: 10pt;
            text-transform: uppercase;
            color: #6b7280;
        }

        .a4-table td {
            padding: 12px;
            border-bottom: 1px solid #e5e7eb;
            font-size: 11pt;
        }

        .a4-totals {
            width: 40%;
            margin-left: auto;
            margin-top: 20px;
        }

        .a4-totals td {
            padding: 8px;
            border-bottom: 1px solid #e5e7eb;
        }

        .a4-totals .total-final {
            font-weight: bold;
            font-size: 14pt;
            color: #2563eb;
            border-top: 2px solid #2563eb;
        }

        /* ==== THERMAL MODE STYLES (Ultra Compact) ==== */
        .page-thermal {
            display: none;
            background: white;
            width: 48mm;
            margin: 0 auto;
            padding: 0;
            font-family: 'Courier New', Courier, monospace;
            font-size: 10px;
            /* Reduced font size */
            line-height: 1.1;
            color: black;
            overflow: hidden;
        }

        .thermal-body {
            width: 100%;
            padding-bottom: 20px;
            /* Space for tear-off */
        }

        .th-center {
            text-align: center;
        }

        .th-right {
            text-align: right;
        }

        .th-bold {
            font-weight: bold;
        }

        .th-line {
            border-top: 1px dashed #000;
            margin: 2px 0;
            width: 100%;
        }

        .th-row {
            display: flex;
            justify-content: space-between;
            margin-bottom: 2px;
        }

        .th-item-name {
            font-weight: bold;
            white-space: nowrap;
            overflow: hidden;
            text-overflow: ellipsis;
            max-width: 100%;
            margin-bottom: 1px;
        }

        /* ==== PRINT MEDIA QUERIES ==== */
        @media print {
            @page {
                margin: 0;
                size: auto;
            }

            /* Critical: remove browser margins */
            .no-print {
                display: none !important;
            }

            body {
                background: white;
            }

            /* A4 Mode Logic */
            body:not(.mode-thermal) .page-thermal {
                display: none !important;
            }

            body:not(.mode-thermal) .page-a4 {
                display: block !important;
                width: 100%;
                margin: 0;
                box-shadow: none;
                page-break-after: always;
            }

            /* Thermal Mode Logic */
            body.mode-thermal .page-a4 {
                display: none !important;
            }

            body.mode-thermal {
                width: 48mm;
            }

            /* Setup body width */
            body.mode-thermal .page-thermal {
                display: block !important;
                width: 100%;
                margin: 0;
                padding: 0;
                box-shadow: none;
                page-break-inside: avoid;
                /* Try to prevent breaks */
            }
        }
    </style>
</head>

<body>
    <!-- CONTROLS -->
    <div class="no-print">
        <button onclick="setMode('a4'); window.print()" class="btn-a4">A4 Print</button>
        <button onclick="setMode('thermal'); window.print()" class="btn-thermal">Browser Print</button>
        <button onclick="printViaRawBT()" class="btn-bluetooth">RawBT/HP (Recommended)</button>
        <button onclick="window.close()" class="btn-close">Close</button>
    </div>

    <!-- VIEW: A4 INVOICE (Standard) -->
    <div class="page-a4" id="view-a4">
        <!-- (Header and content same as before for A4) -->
        <div class="a4-header">
            <div>
                <div class="a4-title">INVOICE</div>
                <div style="color: #6b7280; margin-top: 5px;">#<%= invoice.invoice_number %>
                </div>
            </div>
            <div style="text-align: right;">
                <div style="font-weight: bold; font-size: 16pt; margin-bottom: 5px;">BILLING SYSTEM</div>
                <div style="font-size: 10pt; color: #4b5563;">Jl. Internet No. 123<br>Indonesia</div>
            </div>
        </div>
        <table style="width: 100%; margin-bottom: 30px;">
            <tr>
                <td style="width: 60%; vertical-align: top;">
                    <strong style="color: #6b7280; font-size: 10pt;">Kepada:</strong><br>
                    <div style="font-size: 12pt; font-weight: bold; margin-top: 2px;">
                        <%= invoice.customer_name %>
                    </div>
                    <div style="font-size: 10pt;">
                        <%= invoice.customer_address %>
                    </div>
                </td>
                <td style="width: 40%; vertical-align: top; text-align: right;">
                    <div style="font-size: 10pt;"><strong>Tgl:</strong>
                        <%= new Date(invoice.created_at).toLocaleDateString('id-ID') %>
                    </div>
                    <div style="font-size: 10pt;">
                        <strong>Status:</strong>
                        <%= invoice.status==='paid' ? 'LUNAS' : 'BELUM LUNAS' %>
                    </div>
                </td>
            </tr>
        </table>
        <table class="a4-table">
            <thead>
                <tr>
                    <th width="5%">No</th>
                    <th width="55%">Deskripsi</th>
                    <th width="20%" style="text-align:right">Harga</th>
                    <th width="20%" style="text-align:right">Total</th>
                </tr>
            </thead>
            <tbody>
                <% if (items && items.length> 0) { items.forEach((item, index)=> { %>
                    <tr>
                        <td>
                            <%= index + 1 %>
                        </td>
                        <td>
                            <%= item.description %>
                        </td>
                        <td align="right">
                            <%= new Intl.NumberFormat('id-ID').format(item.unit_price) %>
                        </td>
                        <td align="right">
                            <%= new Intl.NumberFormat('id-ID').format(item.total_price) %>
                        </td>
                    </tr>
                    <% }); } %>
            </tbody>
        </table>
        <table class="a4-totals">
            <tr class="total-final">
                <td>TOTAL</td>
                <td align="right">Rp <%= new Intl.NumberFormat('id-ID').format(invoice.total_amount) %>
                </td>
            </tr>
        </table>
    </div>

    <!-- VIEW: THERMAL / STRUK (58MM) - ULTRA COMPACT -->
    <div class="page-thermal" id="thermal-content">
        <div class="thermal-body">
            <!-- Header -->
            <div class="th-center font-bold" style="font-size: 12px; margin-bottom: 2px;">WIFI BILLING</div>
            <div class="th-center" style="font-size: 9px; margin-bottom: 5px;">0812-3456-7890</div>

            <div class="th-line"></div>

            <!-- Date & Inv -->
            <div class="th-row">
                <span>
                    <%= new Date().toLocaleDateString('id-ID', {day: 'numeric' , month: 'numeric' , year: 'numeric' })
                        %>
                </span>
                <span>#<%= invoice.invoice_number.split('/').pop() /* Short ID */ %></span>
            </div>

            <!-- Customer -->
            <div style="margin-bottom: 2px;">
                Pel: <span class="th-bold">
                    <%= invoice.customer_name.substring(0, 18) %>
                </span>
            </div>

            <div class="th-line"></div>

            <!-- Items -->
            <% if (items && items.length> 0) { %>
                <% items.forEach((item)=> { %>
                    <div style="margin-bottom: 2px;">
                        <div class="th-item-name">
                            <%= item.description %>
                        </div>
                        <div class="th-row">
                            <span>
                                <%= item.quantity || 1 %>x <%= new Intl.NumberFormat('id-ID').format(item.unit_price) %>
                            </span>
                            <span class="th-bold">
                                <%= new Intl.NumberFormat('id-ID').format(item.total_price) %>
                            </span>
                        </div>
                    </div>
                    <% }); %>
                        <% } %>

                            <div class="th-line"></div>

                            <!-- Total -->
                            <div class="th-row" style="margin-top: 2px;">
                                <span class="th-bold" style="font-size: 12px;">TOTAL</span>
                                <span class="th-bold" style="font-size: 12px;">Rp<%= new
                                        Intl.NumberFormat('id-ID').format(invoice.total_amount) %></span>
                            </div>

                            <% if (invoice.remaining_amount> 0) { %>
                                <div class="th-row">
                                    <span>Sisa Tagihan</span>
                                    <span style="color:red">Rp<%= new
                                            Intl.NumberFormat('id-ID').format(invoice.remaining_amount) %></span>
                                </div>
                                <% } %>

                                    <div class="th-line"></div>

                                    <div class="th-center" style="margin-top: 10px; font-size: 9px;">
                                        * TERIMA KASIH *
                                    </div>
        </div>
    </div>

    <script>
        function setMode(mode) {
            if (mode === 'thermal') {
                document.body.classList.add('mode-thermal');
                document.getElementById('view-a4').style.display = 'none';
                document.getElementById('thermal-content').style.display = 'block';
            } else {
                document.body.classList.remove('mode-thermal');
                document.getElementById('view-a4').style.display = 'block';
                document.getElementById('thermal-content').style.display = 'none';
            }
        }

        function printViaRawBT() {
            setMode('thermal');
            var content = document.getElementById('thermal-content').innerHTML;
            // Add forced inline CSS for RawBT to respect widths
            var styledContent = '<div style="font-family:monospace; width:48mm; font-size:10px;">' + content + '</div>';
            var base64 = btoa(unescape(encodeURIComponent(styledContent)));
            window.location.href = 'rawbt:data:text/html;base64,' + base64;
        }

        // Default mobile to thermal
        if (window.innerWidth < 600) setMode('thermal');
    </script>
</body>

</html>