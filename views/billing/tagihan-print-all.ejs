<!DOCTYPE html>
<html lang="id">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>
        <%= title %>
    </title>
    <style>
        @page {
            size: 58mm auto;
            margin: 1mm 2mm;
        }

        @media print {
            body {
                -webkit-print-color-adjust: exact;
                print-color-adjust: exact;
                width: 58mm;
            }

            .no-print {
                display: none !important;
            }

            .page-break {
                page-break-after: always;
            }
        }

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Courier New', monospace;
            font-size: 8pt;
            line-height: 1.3;
            color: #000;
            width: 58mm;
            max-width: 58mm;
            font-weight: 600;
        }

        .receipt {
            padding: 2mm;
            margin-bottom: 5mm;
        }

        .header {
            text-align: center;
            padding-bottom: 2mm;
        }

        .company-name {
            font-size: 11pt;
            font-weight: bold;
            text-transform: uppercase;
            letter-spacing: 0.5px;
        }

        .company-detail {
            font-size: 6.5pt;
            line-height: 1.4;
            margin-top: 1mm;
        }

        .sep {
            border: none;
            border-top: 1px dashed #000;
            margin: 2mm 0;
        }

        .sep-bold {
            border: none;
            border-top: 2px solid #000;
            margin: 2mm 0;
        }

        .cut-line {
            text-align: center;
            margin: 5mm 0;
            position: relative;
        }

        .cut-line::before {
            content: '';
            border-top: 2px dashed #000;
            width: 100%;
            position: absolute;
            top: 50%;
            left: 0;
        }

        .cut-line span {
            background: #fff;
            padding: 0 8px;
            position: relative;
            z-index: 1;
            font-size: 7pt;
            font-weight: bold;
        }

        .cut-counter {
            font-size: 6.5pt;
            margin-top: 2mm;
            font-weight: normal;
            text-align: center;
        }

        .row {
            display: flex;
            justify-content: space-between;
            margin-bottom: 1mm;
            font-size: 7.5pt;
        }

        .row .lbl {
            font-weight: normal;
            flex-shrink: 0;
        }

        .row .val {
            font-weight: bold;
            text-align: right;
            word-break: break-word;
            max-width: 58%;
        }

        .section-title {
            font-size: 7.5pt;
            font-weight: bold;
            text-transform: uppercase;
            margin-bottom: 1mm;
            letter-spacing: 0.5px;
            text-decoration: underline;
        }

        .items-table {
            width: 100%;
            border-collapse: collapse;
            font-size: 7pt;
        }

        .items-table th {
            font-weight: bold;
            padding: 1mm 0;
            text-align: left;
            font-size: 7pt;
            border-bottom: 1px dashed #000;
        }

        .items-table td {
            padding: 1.5mm 0;
            vertical-align: top;
            font-size: 7pt;
        }

        .items-table .col-desc {
            width: 55%;
        }

        .items-table .col-price {
            width: 45%;
            text-align: right;
            font-weight: bold;
        }

        .total-row {
            display: flex;
            justify-content: space-between;
            font-size: 7.5pt;
            margin-bottom: 1mm;
        }

        .grand-total {
            border-top: 2px solid #000;
            padding-top: 2mm;
            margin-top: 1mm;
        }

        .grand-total .total-row {
            font-size: 10pt;
            font-weight: bold;
        }

        .paid-stamp {
            text-align: center;
            border: 2px solid #000;
            padding: 2mm;
            margin: 2mm 0;
            font-size: 10pt;
            font-weight: bold;
            letter-spacing: 2px;
        }

        .info-box {
            font-size: 6.5pt;
            padding: 2mm;
            border: 1px dashed #000;
            line-height: 1.4;
            margin: 2mm 0;
        }

        .footer {
            text-align: center;
            font-size: 6pt;
            padding-top: 2mm;
            line-height: 1.4;
        }

        .print-controls {
            text-align: center;
            margin-bottom: 15px;
            padding: 15px;
            background: white;
            border-radius: 8px;
            box-shadow: 0 2px 8px rgba(0, 0, 0, 0.1);
        }

        .btn-print {
            background: #007bff;
            color: white;
            padding: 10px 20px;
            border: none;
            border-radius: 5px;
            cursor: pointer;
            font-size: 14px;
            font-weight: 700;
            margin: 5px;
        }

        .btn-print:hover {
            background: #0056b3;
        }

        @media screen {
            body {
                width: auto;
                max-width: 400px;
                margin: 0 auto;
                padding: 20px;
                background: #f0f0f0;
            }

            .receipt {
                background: white;
                padding: 15px;
                margin-bottom: 10px;
                box-shadow: 0 4px 20px rgba(0, 0, 0, 0.12);
                border-radius: 4px;
            }
        }
    </style>
</head>

<body>
    <!-- Print Controls -->
    <div class="print-controls no-print">
        <button id="btnPrintAll" class="btn-print">
            🖨️ Print Semua Invoice (58mm)
        </button>
        <button id="btnClosePage" class="btn-print" style="background: #6c757d;">
            ✖️ Tutup
        </button>
        <p style="margin-top: 12px; font-size: 13px; font-weight: 600; color: #333;">
            📄 Total: <strong style="color: #28a745;">
                <%= invoices ? invoices.length : 0 %> Invoice
            </strong>
        </p>
    </div>

    <% if (invoices && invoices.length> 0) { %>
        <% // Group invoices by ODC const groupedInvoices={}; invoices.forEach(invoice=> {
            const odcKey = invoice.odc_name || 'Tanpa ODC/ODP';
            if (!groupedInvoices[odcKey]) {
            groupedInvoices[odcKey] = [];
            }
            groupedInvoices[odcKey].push(invoice);
            });

            let invoiceCounter = 0;
            %>

            <% Object.keys(groupedInvoices).forEach((odcName, odcIndex)=> { %>
                <% groupedInvoices[odcName].forEach((invoice, invIndex)=> { %>
                    <% invoiceCounter++; %>
                        <div class="receipt">
                            <!-- PAID STAMP -->
                            <% if (invoice.status==='paid' || (invoice.paid_amount && parseFloat(invoice.paid_amount)>=
                                parseFloat(invoice.total_amount))) { %>
                                <div class="paid-stamp">
                                    ★ LUNAS / PAID ★<br>
                                    <span style="font-size: 7pt;">ADMIN FINANCE</span>
                                </div>
                                <% } %>

                                    <!-- Header -->
                                    <div class="header">
                                        <div class="company-name">
                                            <%= (typeof companyInfo !=='undefined' && companyInfo &&
                                                companyInfo.company_name) ? companyInfo.company_name : 'BILLING SYSTEM'
                                                %>
                                        </div>
                                        <div class="company-detail">
                                            <% if (typeof companyInfo !=='undefined' && companyInfo) { %>
                                                <% if (companyInfo.company_phone) { %>Telp/WA: <%=
                                                        companyInfo.company_phone %><br>
                                                        <% } %>
                                                            <% if (companyInfo.company_address) { %>
                                                                <%= companyInfo.company_address %>
                                                                    <% } %>
                                                                        <% } %>
                                        </div>
                                    </div>

                                    <hr class="sep-bold">

                                    <!-- Date & Ref -->
                                    <div class="row">
                                        <span class="lbl">TGL: <%= new
                                                Date(invoice.created_at).toLocaleDateString('id-ID', {day: '2-digit' ,
                                                month: '2-digit' , year: 'numeric' }) %></span>
                                        <span class="val">PKL: <%= new Date().toLocaleTimeString('id-ID',
                                                {hour: '2-digit' , minute: '2-digit' }) %></span>
                                    </div>
                                    <div class="row">
                                        <span class="lbl">REF: #<%= invoice.invoice_number %></span>
                                    </div>

                                    <hr class="sep">

                                    <!-- Customer Info -->
                                    <div class="section-title">PELANGGAN</div>
                                    <div class="row">
                                        <span class="lbl">ID PEL</span>
                                        <span class="val">
                                            <%= invoice.customer_code || ('PEL-' + invoice.customer_id) %>
                                        </span>
                                    </div>
                                    <div class="row">
                                        <span class="lbl">Nama</span>
                                        <span class="val">
                                            <%= invoice.customer_name %>
                                        </span>
                                    </div>
                                    <div class="row">
                                        <span class="lbl">Periode</span>
                                        <span class="val">
                                            <% const periodParts=invoice.period.split('-'); const year=periodParts[0];
                                                const monthNum=parseInt(periodParts[1]); const
                                                monthNames=['Januari','Februari','Maret','April','Mei','Juni','Juli','Agustus','September','Oktober','November','Desember'];
                                                const monthName=monthNames[monthNum - 1] || invoice.period; %>
                                                <%= monthName %>
                                                    <%= year %>
                                        </span>
                                    </div>
                                    <div class="row">
                                        <span class="lbl">Jatuh Tempo</span>
                                        <span class="val">
                                            <%= new Date(invoice.due_date).toLocaleDateString('id-ID', {day: '2-digit' ,
                                                month: '2-digit' , year: 'numeric' }) %>
                                        </span>
                                    </div>
                                    <% if (invoice.customer_address) { %>
                                        <div class="row">
                                            <span class="lbl">Alamat</span>
                                            <span class="val">
                                                <%= invoice.customer_address %>
                                            </span>
                                        </div>
                                        <% } %>
                                            <% if (invoice.odc_name) { %>
                                                <div class="row">
                                                    <span class="lbl">Area</span>
                                                    <span class="val">
                                                        <%= invoice.odc_name %>
                                                    </span>
                                                </div>
                                                <% } %>

                                                    <hr class="sep">

                                                    <!-- Items -->
                                                    <div class="section-title">RINCIAN TAGIHAN</div>
                                                    <table class="items-table">
                                                        <thead>
                                                            <tr>
                                                                <th class="col-desc">Layanan</th>
                                                                <th class="col-price">Harga</th>
                                                            </tr>
                                                        </thead>
                                                        <tbody>
                                                            <% if (invoice.items && invoice.items.length> 0) { %>
                                                                <% invoice.items.forEach(item=> { %>
                                                                    <tr>
                                                                        <td class="col-desc">
                                                                            <%= item.description %><br>
                                                                                <span
                                                                                    style="font-size: 6.5pt; color: #555;">
                                                                                    <%= item.quantity || 1 %> x Rp <%=
                                                                                            new
                                                                                            Intl.NumberFormat('id-ID').format(item.unit_price)
                                                                                            %>
                                                                                </span>
                                                                        </td>
                                                                        <td class="col-price">Rp <%= new
                                                                                Intl.NumberFormat('id-ID').format(item.total_price)
                                                                                %>
                                                                        </td>
                                                                    </tr>
                                                                    <% }); %>
                                                                        <% } else { %>
                                                                            <tr>
                                                                                <td class="col-desc">Layanan
                                                                                    Internet<br><span
                                                                                        style="font-size: 6.5pt; color: #555;">
                                                                                        <%= monthName %>
                                                                                            <%= year %>
                                                                                    </span></td>
                                                                                <td class="col-price">Rp <%= new
                                                                                        Intl.NumberFormat('id-ID').format(invoice.subtotal
                                                                                        || invoice.total_amount) %>
                                                                                </td>
                                                                            </tr>
                                                                            <% } %>
                                                        </tbody>
                                                    </table>

                                                    <hr class="sep">

                                                    <!-- Totals -->
                                                    <div class="total-row">
                                                        <span>Subtotal</span>
                                                        <span style="font-weight: bold;">Rp <%= new
                                                                Intl.NumberFormat('id-ID').format(invoice.subtotal ||
                                                                invoice.total_amount) %></span>
                                                    </div>
                                                    <% if (invoice.discount_amount && invoice.discount_amount> 0) { %>
                                                        <div class="total-row" style="color: #d9534f;">
                                                            <span>Diskon<% if (invoice.discount_reason) { %> (<%=
                                                                        invoice.discount_reason %>)<% } %></span>
                                                            <span style="font-weight: bold;">- Rp <%= new
                                                                    Intl.NumberFormat('id-ID').format(invoice.discount_amount)
                                                                    %></span>
                                                        </div>
                                                        <% } %>
                                                            <div class="total-row">
                                                                <span>PPN</span>
                                                                <span>Rp 0</span>
                                                            </div>

                                                            <div class="grand-total">
                                                                <div class="total-row">
                                                                    <span>TOTAL BAYAR</span>
                                                                    <span>Rp <%= new
                                                                            Intl.NumberFormat('id-ID').format(invoice.total_amount)
                                                                            %></span>
                                                                </div>
                                                            </div>

                                                            <% if (invoice.paid_amount> 0) { %>
                                                                <div class="total-row" style="margin-top: 1mm;">
                                                                    <span>Sudah Dibayar</span>
                                                                    <span style="font-weight: bold; color: #2e7d32;">Rp
                                                                        <%= new
                                                                            Intl.NumberFormat('id-ID').format(invoice.paid_amount)
                                                                            %>
                                                                    </span>
                                                                </div>
                                                                <div class="total-row">
                                                                    <span style="font-weight: bold;">Sisa Piutang</span>
                                                                    <span style="font-weight: bold; color: #d32f2f;">Rp
                                                                        <%= new
                                                                            Intl.NumberFormat('id-ID').format(invoice.remaining_amount
                                                                            || (invoice.total_amount -
                                                                            invoice.paid_amount)) %>
                                                                    </span>
                                                                </div>
                                                                <% } %>

                                                                    <hr class="sep">

                                                                    <!-- Info Box -->
                                                                    <div class="info-box">
                                                                        <div
                                                                            style="font-weight: bold; margin-bottom: 1mm; text-decoration: underline;">
                                                                            INFORMASI LAYANAN:</div>
                                                                        • Simpan struk sebagai bukti bayar sah.<br>
                                                                        • Bayar tepat waktu agar tidak terisolir.
                                                                    </div>

                                                                    <!-- QR Code -->
                                                                    <div style="text-align: center; margin: 2mm 0;">
                                                                        <img src="https://api.qrserver.com/v1/create-qr-code/?size=80x80&data=<%= encodeURIComponent('VERIFIED OK: ' + invoice.invoice_number + ' | ' + invoice.customer_name + ' | ' + invoice.total_amount) %>"
                                                                            style="width: 50px; height: 50px;" alt="QR">
                                                                        <div style="font-size: 5.5pt; margin-top: 1mm;">
                                                                            E-RECEIPT VERIFICATION</div>
                                                                    </div>

                                                                    <!-- Footer -->
                                                                    <div class="footer">
                                                                        *** TERIMA KASIH ***<br>
                                                                        <% if (invoice.odc_name) { %>Area: <%=
                                                                                invoice.odc_name %><br>
                                                                                <% } %>
                                                                                    <%= new
                                                                                        Date().toLocaleString('id-ID',
                                                                                        {day: '2-digit' ,
                                                                                        month: '2-digit' ,
                                                                                        year: 'numeric' ,
                                                                                        hour: '2-digit' ,
                                                                                        minute: '2-digit' }) %>
                                                                    </div>
                        </div>

                        <!-- Cut line separator between invoices -->
                        <% if (invoiceCounter < invoices.length) { %>
                            <div class="cut-line page-break">
                                <span>✂ SOBEK DISINI</span>
                            </div>
                            <div class="cut-counter no-print">Invoice <%= invoiceCounter + 1 %> / <%= invoices.length %>
                            </div>
                            <% } %>
                                <% }); %>
                                    <% }); %>
                                        <% } else { %>
                                            <div style="text-align: center; padding: 20mm;">
                                                <p style="font-size: 10pt; font-weight: bold;">Tidak ada tagihan</p>
                                                <p style="font-size: 8pt; margin-top: 5mm;">Tidak ada data tagihan yang
                                                    ditemukan</p>
                                            </div>
                                            <% } %>

                                                <script>
                                                    document.addEventListener('DOMContentLoaded', function () {
                                                        const btnPrint = document.getElementById('btnPrintAll');
                                                        if (btnPrint) {
                                                            btnPrint.addEventListener('click', function () {
                                                                window.print();
                                                            });
                                                        }

                                                        const btnClose = document.getElementById('btnClosePage');
                                                        if (btnClose) {
                                                            btnClose.addEventListener('click', function () {
                                                                window.close();
                                                            });
                                                        }
                                                    });
                                                </script>
</body>

</html>