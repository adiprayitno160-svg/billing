<!DOCTYPE html>
<html lang="id">

<head>
    <meta charset="UTF-8">
    <title>Invoice #<%= invoice.invoice_number %></title>
    <style>
        body {
            font-family: Arial, sans-serif;
            font-size: 8px;
            margin: 5px;
            background-color: #fff;
            color: #1e293b;
        }
        
        .stamp-container {
            position: absolute;
            top: 15%;
            left: 50%;
            transform: translate(-50%, -50%) rotate(-8deg);
            z-index: 10;
            opacity: 0.6;
            pointer-events: none;
        }

        .stamp-box {
            border: 1px solid #16a34a;
            color: #16a34a;
            padding: 2px 8px;
            text-align: center;
            border-radius: 2px;
            background-color: rgba(255, 255, 255, 0.8);
            font-size: 10px;
            font-weight: bold;
            letter-spacing: 0.05rem;
            text-transform: uppercase;
        }

        .stamp-date {
            font-size: 6px;
            font-weight: bold;
            margin-top: 1px;
            border-top: 1px solid #16a34a;
            padding-top: 1px;
        }
        
        .header {
            display: flex;
            justify-content: space-between;
            margin-bottom: 5px;
            padding-bottom: 3px;
            border-bottom: 1px solid #ccc;
        }
        
        .company-info h1 {
            font-size: 10px;
            font-weight: bold;
            margin-bottom: 1px;
        }
        
        .company-info p {
            font-size: 7px;
            margin: 1px 0;
        }
        
        .invoice-title {
            text-align: right;
        }
        
        .invoice-title h2 {
            font-size: 12px;
            color: #ddd;
            font-weight: bold;
            margin-bottom: 1px;
        }
        
        .invoice-number, .invoice-dates {
            font-size: 7px;
        }
        
        .bill-to {
            background-color: #f8fafc;
            padding: 4px;
            margin-bottom: 5px;
            border: 1px solid #e2e8f0;
            border-radius: 2px;
        }
        
        .bill-header {
            font-size: 6px;
            font-weight: bold;
            text-transform: uppercase;
            color: #94a3b8;
            margin-bottom: 3px;
        }
        
        .bill-content {
            display: flex;
            justify-content: space-between;
        }
        
        .customer-info h4 {
            font-size: 9px;
            font-weight: bold;
            margin-bottom: 1px;
        }
        
        .customer-details {
            font-size: 7px;
            margin: 1px 0;
        }
        
        .username-ip {
            text-align: right;
            font-size: 7px;
        }
        
        .username-ip div {
            margin-bottom: 1px;
        }
        
        .item-label {
            font-size: 5px;
            font-weight: bold;
            text-transform: uppercase;
            color: #64748b;
        }
        
        table {
            width: 100%;
            border-collapse: collapse;
            margin-bottom: 5px;
        }
        
        th {
            text-align: left;
            padding: 2px 1px;
            font-size: 6px;
            font-weight: bold;
            color: #64748b;
            border-bottom: 1px solid #cbd5e1;
        }
        
        td {
            padding: 2px 1px;
            font-size: 7px;
            border-bottom: 1px solid #e2e8f0;
        }
        
        .desc-col {
            text-align: left;
        }
        
        .price-col {
            text-align: right;
        }
        
        .amount-col {
            text-align: right;
            font-weight: bold;
        }
        
        tfoot tr {
            border-top: 1px solid #cbd5e1;
        }
        
        .total-row {
            font-weight: bold;
            font-size: 9px;
        }
        
        .paid-row {
            color: #16a34a;
            font-weight: bold;
        }
        
        .remaining-row {
            font-weight: bold;
            font-size: 8px;
        }
        
        .footer {
            margin-top: 8px;
            padding-top: 4px;
            border-top: 1px solid #cbd5e1;
            display: flex;
            justify-content: space-between;
            font-size: 7px;
        }
        
        .payment-info {
            color: #64748b;
        }
        
        .signature {
            text-align: right;
        }
        
        .signature-title {
            color: #94a3b8;
            font-size: 6px;
            margin-bottom: 8px;
        }
        
        .signature-name {
            font-weight: bold;
            font-size: 9px;
        }
    </style>
</head>

<body>

    <% if (invoice.status==='paid' ) { %>
        <div class="stamp-container">
            <div class="stamp-box">
                <div>LUNAS</div>
                <div class="stamp-date">
                    TGL: <%= invoice.paid_at ? new Date(invoice.paid_at).toLocaleDateString('id-ID', {day: '2-digit', month: 'short', year: 'numeric'}) : new Date().toLocaleDateString('id-ID') %>
                </div>
            </div>
        </div>
    <% } %>

    <div class="header">
        <div class="company-info">
            <h1><%= invoice.company_name || 'ISP COMPANY' %></h1>
            <p class="whitespace-pre-line"><%= invoice.company_address || 'Jl. Raya Internet No. 1\nJakarta, Indonesia' %></p>
            <p>Telp: <%= invoice.company_phone || '-' %></p>
        </div>
        <div class="invoice-title">
            <h2>INVOICE</h2>
            <p class="invoice-number">#<%= invoice.invoice_number %></p>
            <p class="invoice-dates">
                Dibuat: <%= new Date(invoice.created_at).toLocaleDateString('id-ID', {day: 'numeric', month: 'short', year: 'numeric'}) %><br>
                Jatuh Tempo: <%= new Date(invoice.due_date).toLocaleDateString('id-ID', {day: 'numeric', month: 'short', year: 'numeric'}) %>
            </p>
        </div>
    </div>

    <div class="bill-to">
        <div class="bill-header">DITAGIHKAN KEPADA</div>
        <div class="bill-content">
            <div class="customer-info">
                <h4><%= invoice.customer_name %></h4>
                <p class="customer-details"><%= invoice.customer_address || 'Alamat tidak tersedia' %></p>
                <% if(invoice.customer_phone) { %>
                    <p class="customer-details">Telp: <%= invoice.customer_phone %></p>
                <% } %>
            </div>
            <div class="username-ip">
                <% if(invoice.pppoe_username) { %>
                    <div>
                        <div class="item-label">Username</div>
                        <span><%= invoice.pppoe_username %></span>
                    </div>
                <% } %>
                <% if(invoice.customer_ip) { %>
                    <div>
                        <div class="item-label">IP Address</div>
                        <span><%= invoice.customer_ip %></span>
                    </div>
                <% } %>
            </div>
        </div>
    </div>

    <table>
        <thead>
            <tr>
                <th class="desc-col">DESKRIPSI</th>
                <th class="price-col" style="width: 70px;">HARGA SATUAN</th>
                <th class="amount-col" style="width: 70px;">TOTAL</th>
            </tr>
        </thead>
        <tbody>
            <% (invoice.items || []).forEach(item => { %>
                <tr>
                    <td class="desc-col">
                        <div><%= item.description %></div>
                        <% if(item.details) { %>
                            <div style="font-size: 6px; color: #64748b;"><%= item.details %></div>
                        <% } %>
                    </td>
                    <td class="price-col">Rp <%= Number(item.unit_price || item.amount).toLocaleString('id-ID') %></td>
                    <td class="amount-col">Rp <%= Number(item.amount).toLocaleString('id-ID') %></td>
                </tr>
            <% }) %>

            <% if (!invoice.items || invoice.items.length === 0) { %>
                <tr>
                    <td class="desc-col">
                        <div>Layanan Internet Period <%= invoice.period_date || '-' %></div>
                    </td>
                    <td class="price-col">Rp <%= Number(invoice.total_amount).toLocaleString('id-ID') %></td>
                    <td class="amount-col">Rp <%= Number(invoice.total_amount).toLocaleString('id-ID') %></td>
                </tr>
            <% } %>
        </tbody>
        <tfoot>
            <tr>
                <td colspan="2" style="text-align: right; font-weight: bold; border-bottom: none;">Total Tagihan</td>
                <td class="total-row">Rp <%= Number(invoice.total_amount).toLocaleString('id-ID') %></td>
            </tr>
            <% if (invoice.paid_amount > 0) { %>
                <tr>
                    <td colspan="2" style="text-align: right; font-weight: bold; border-bottom: none;" class="paid-row">Dibayar</td>
                    <td class="paid-row">- Rp <%= Number(invoice.paid_amount).toLocaleString('id-ID') %></td>
                </tr>
                <tr>
                    <td colspan="2" style="text-align: right; font-weight: bold; border-bottom: none;">Sisa Tagihan</td>
                    <td class="remaining-row">Rp <%= Number(invoice.remaining_amount).toLocaleString('id-ID') %></td>
                </tr>
            <% } %>
        </tfoot>
    </table>

    <div class="footer">
        <div class="payment-info">
            <div style="font-weight: bold; color: #334155; margin-bottom: 2px;">Metode Pembayaran:</div>
            <div>Silakan transfer ke nomor rekening yang tertera di aplikasi/website.</div>
            <div style="margin-top: 2px;"><em>Terima kasih atas kepercayaan Anda.</em></div>
        </div>
        <div class="signature">
            <div class="signature-title">Hormat Kami,</div>
            <div class="signature-name"><%= invoice.company_name || 'Admin Finance' %></div>
        </div>
    </div>

</body>

</html>