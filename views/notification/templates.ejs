<!DOCTYPE html>
<html lang="id">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Template Notifikasi - Billing System</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
</head>

<body class="bg-gray-50">
    <%- include('../partials/navbar') %>

        <div class="flex">
            <%- include('../partials/sidebar') %>

                <main class="flex-1 p-8 max-w-7xl mx-auto w-full">
                    <div class="space-y-6">
                        <!-- Header -->
                        <div class="mb-6 flex flex-col sm:flex-row sm:items-center sm:justify-between gap-4">
                            <div>
                                <h1 class="text-3xl font-bold text-gray-900 flex items-center gap-3">
                                    <i class="fas fa-envelope text-blue-500"></i>
                                    Template Notifikasi
                                </h1>
                                <p class="text-gray-600 mt-2">Kelola template notifikasi untuk WhatsApp, Email, SMS, dan
                                    Push</p>
                            </div>
                            <a href="/notification/templates/create"
                                class="px-4 py-2 bg-blue-500 text-white rounded-lg hover:bg-blue-600 transition flex items-center gap-2 shadow-sm whitespace-nowrap">
                                <i class="fas fa-plus"></i> Buat Template Baru
                            </a>
                        </div>

                        <!-- Info Alert -->
                        <div class="bg-blue-50 border border-blue-200 rounded-lg p-4 mb-6 shadow-sm">
                            <div class="flex items-start">
                                <i class="fas fa-info-circle text-blue-500 mt-1 mr-3 text-lg"></i>
                                <div class="text-sm text-blue-800">
                                    <p class="font-semibold mb-1 text-base">Cara Menggunakan Template:</p>
                                    <ul class="list-disc list-inside space-y-1 ml-1">
                                        <li>Gunakan variabel seperti <code
                                                class="bg-blue-100 px-1.5 py-0.5 rounded text-blue-900 font-mono text-xs">{customer_name}</code>,
                                            <code
                                                class="bg-blue-100 px-1.5 py-0.5 rounded text-blue-900 font-mono text-xs">{invoice_number}</code>,
                                            dll.</li>
                                        <li>Template akan otomatis digunakan saat notifikasi dikirim berdasarkan tipe
                                            dan channel.</li>
                                        <li>Edit template untuk menyesuaikan pesan sesuai kebutuhan.</li>
                                    </ul>
                                </div>
                            </div>
                        </div>

                        <!-- Statistics -->
                        <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-4 mb-6">
                            <div class="bg-white rounded-lg shadow-sm p-4 border border-gray-100">
                                <div class="text-sm text-gray-500 font-medium mb-1">Total Template</div>
                                <div class="text-2xl font-bold text-gray-900">
                                    <%= templates.length %>
                                </div>
                            </div>
                            <div class="bg-white rounded-lg shadow-sm p-4 border border-gray-100">
                                <div class="text-sm text-gray-500 font-medium mb-1">Notifikasi Terkirim</div>
                                <div class="text-2xl font-bold text-green-600">
                                    <%= stats.sent || 0 %>
                                </div>
                            </div>
                            <div class="bg-white rounded-lg shadow-sm p-4 border border-gray-100">
                                <div class="text-sm text-gray-500 font-medium mb-1">Notifikasi Gagal</div>
                                <div class="text-2xl font-bold text-red-600">
                                    <%= stats.failed || 0 %>
                                </div>
                            </div>
                            <div class="bg-white rounded-lg shadow-sm p-4 border border-gray-100">
                                <div class="text-sm text-gray-500 font-medium mb-1">Success Rate</div>
                                <div class="text-2xl font-bold text-blue-600">
                                    <%= stats.total> 0 ? ((stats.sent / stats.total) * 100).toFixed(1) : '0' %>%
                                </div>
                            </div>
                        </div>

                        <!-- Filters -->
                        <div class="bg-white rounded-lg shadow-sm p-6 mb-6 border border-gray-100">
                            <div class="flex flex-col lg:flex-row gap-4 items-start lg:items-center justify-between">
                                <div class="flex flex-wrap gap-3 flex-1">
                                    <select id="filterType"
                                        class="px-4 py-2 border border-gray-300 rounded-lg focus:ring-2 focus:ring-blue-500 text-sm focus:border-blue-500 bg-white">
                                        <option value="">Semua Tipe</option>
                                        <option value="invoice_created">Invoice Dibuat</option>
                                        <option value="invoice_overdue">Invoice Terlambat</option>
                                        <option value="payment_received">Pembayaran Diterima</option>
                                        <option value="package_expiring">Paket Akan Berakhir</option>
                                        <option value="package_expired">Paket Berakhir</option>
                                        <option value="package_activated">Paket Diaktifkan</option>
                                    </select>
                                    <select id="filterChannel"
                                        class="px-4 py-2 border border-gray-300 rounded-lg focus:ring-2 focus:ring-blue-500 text-sm focus:border-blue-500 bg-white">
                                        <option value="">Semua Channel</option>
                                        <option value="whatsapp">WhatsApp</option>
                                        <option value="email">Email</option>
                                        <option value="sms">SMS</option>
                                        <option value="push">Push</option>
                                    </select>
                                    <select id="filterActive"
                                        class="px-4 py-2 border border-gray-300 rounded-lg focus:ring-2 focus:ring-blue-500 text-sm focus:border-blue-500 bg-white">
                                        <option value="">Semua Status</option>
                                        <option value="true">Aktif</option>
                                        <option value="false">Tidak Aktif</option>
                                    </select>
                                </div>
                                <div class="flex gap-2 w-full lg:w-auto">
                                    <button onclick="applyFilters()"
                                        class="flex-1 lg:flex-none px-4 py-2 bg-blue-500 text-white rounded-lg hover:bg-blue-600 transition shadow-sm text-sm font-medium">
                                        <i class="fas fa-filter mr-1"></i> Filter
                                    </button>
                                    <button onclick="resetFilters()"
                                        class="flex-1 lg:flex-none px-4 py-2 bg-gray-100 text-gray-700 rounded-lg hover:bg-gray-200 transition text-sm font-medium">
                                        <i class="fas fa-redo mr-1"></i> Reset
                                    </button>
                                </div>
                            </div>
                        </div>

                        <!-- Templates List -->
                        <div class="bg-white rounded-lg shadow-sm overflow-hidden border border-gray-200">
                            <div class="overflow-x-auto">
                                <table class="min-w-full divide-y divide-gray-200">
                                    <thead class="bg-gray-50">
                                        <tr>
                                            <th
                                                class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">
                                                Kode</th>
                                            <th
                                                class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">
                                                Nama</th>
                                            <th
                                                class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">
                                                Tipe</th>
                                            <th
                                                class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">
                                                Channel</th>
                                            <th
                                                class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">
                                                Status</th>
                                            <th
                                                class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">
                                                Priority</th>
                                            <th
                                                class="px-6 py-3 text-right text-xs font-medium text-gray-500 uppercase tracking-wider">
                                                Aksi</th>
                                        </tr>
                                    </thead>
                                    <tbody class="bg-white divide-y divide-gray-200" id="templatesTableBody">
                                        <% if (templates.length===0) { %>
                                            <tr>
                                                <td colspan="7" class="px-6 py-10 text-center text-gray-500">
                                                    <i class="fas fa-file-alt text-4xl mb-3 text-gray-300 block"></i>
                                                    Tidak ada template yang ditemukan.
                                                </td>
                                            </tr>
                                            <% } %>
                                                <% templates.forEach(template=> { %>
                                                    <tr class="hover:bg-gray-50 transition-colors">
                                                        <td
                                                            class="px-6 py-4 whitespace-nowrap text-sm font-medium text-gray-900">
                                                            <code
                                                                class="bg-gray-100 px-2 py-1 rounded text-purple-600 font-mono border border-gray-200"><%= template.template_code %></code>
                                                        </td>
                                                        <td class="px-6 py-4 text-sm text-gray-900">
                                                            <%= template.template_name %>
                                                        </td>
                                                        <td class="px-6 py-4 whitespace-nowrap text-sm text-gray-600">
                                                            <span
                                                                class="px-2 py-1 bg-blue-50 text-blue-700 border border-blue-100 rounded text-xs font-medium">
                                                                <%= template.notification_type %>
                                                            </span>
                                                        </td>
                                                        <td class="px-6 py-4 whitespace-nowrap text-sm">
                                                            <span class="px-2 py-1 rounded text-xs font-medium flex items-center w-fit gap-1.5
                                                <%= template.channel === 'whatsapp' ? 'bg-green-50 text-green-700 border border-green-100' : 
                                                    template.channel === 'email' ? 'bg-blue-50 text-blue-700 border border-blue-100' : 
                                                    template.channel === 'sms' ? 'bg-yellow-50 text-yellow-700 border border-yellow-100' : 
                                                    'bg-purple-50 text-purple-700 border border-purple-100' %>">
                                                                <i
                                                                    class="fab fa-<%= template.channel === 'email' ? 'google' : template.channel %>"></i>
                                                                <%= template.channel.toUpperCase() %>
                                                            </span>
                                                        </td>
                                                        <td class="px-6 py-4 whitespace-nowrap text-sm">
                                                            <% if (template.is_active) { %>
                                                                <span
                                                                    class="px-2 py-1 bg-green-100 text-green-800 rounded-full text-xs font-medium inline-flex items-center gap-1">
                                                                    <span
                                                                        class="w-1.5 h-1.5 bg-green-500 rounded-full"></span>
                                                                    Aktif
                                                                </span>
                                                                <% } else { %>
                                                                    <span
                                                                        class="px-2 py-1 bg-gray-100 text-gray-600 rounded-full text-xs font-medium inline-flex items-center gap-1">
                                                                        <span
                                                                            class="w-1.5 h-1.5 bg-gray-400 rounded-full"></span>
                                                                        Tidak Aktif
                                                                    </span>
                                                                    <% } %>
                                                        </td>
                                                        <td class="px-6 py-4 whitespace-nowrap text-sm">
                                                            <span class="px-2 py-1 rounded text-xs font-medium inline-flex items-center gap-1
                                                <%= template.priority === 'high' ? 'bg-red-50 text-red-700 border border-red-100' : 
                                                    template.priority === 'normal' ? 'bg-blue-50 text-blue-700 border border-blue-100' : 
                                                    'bg-gray-50 text-gray-600 border border-gray-100' %>">
                                                                <% if(template.priority==='high' ) { %><i
                                                                        class="fas fa-arrow-up text-[10px]"></i>
                                                                    <% } %>
                                                                        <%= template.priority %>
                                                            </span>
                                                        </td>
                                                        <td
                                                            class="px-6 py-4 whitespace-nowrap text-sm font-medium text-right">
                                                            <div class="flex justify-end gap-2">
                                                                <a href="/notification/templates/edit/<%= template.template_code %>"
                                                                    class="p-1.5 text-blue-600 hover:text-blue-900 hover:bg-blue-50 rounded transition-colors"
                                                                    title="Edit">
                                                                    <i class="fas fa-edit"></i>
                                                                </a>
                                                                <button
                                                                    onclick="viewTemplate('<%= template.template_code %>')"
                                                                    class="p-1.5 text-green-600 hover:text-green-900 hover:bg-green-50 rounded transition-colors"
                                                                    title="Lihat">
                                                                    <i class="fas fa-eye"></i>
                                                                </button>
                                                                <button
                                                                    onclick="toggleActive('<%= template.template_code %>', <%= !template.is_active %>)"
                                                                    class="p-1.5 text-<%= template.is_active ? 'amber' : 'purple' %>-600 hover:text-<%= template.is_active ? 'amber' : 'purple' %>-900 hover:bg-<%= template.is_active ? 'amber' : 'purple' %>-50 rounded transition-colors"
                                                                    title="<%= template.is_active ? 'Nonaktifkan' : 'Aktifkan' %>">
                                                                    <i
                                                                        class="fas fa-<%= template.is_active ? 'pause' : 'play' %>"></i>
                                                                </button>
                                                                <button
                                                                    onclick="deleteTemplate('<%= template.template_code %>')"
                                                                    class="p-1.5 text-red-600 hover:text-red-900 hover:bg-red-50 rounded transition-colors"
                                                                    title="Hapus">
                                                                    <i class="fas fa-trash"></i>
                                                                </button>
                                                            </div>
                                                        </td>
                                                    </tr>
                                                    <% }); %>
                                    </tbody>
                                </table>
                            </div>
                        </div>
                    </div>
                </main>
        </div>

        <!-- Template Details Modal -->
        <div id="templateModal"
            class="hidden fixed inset-0 bg-black bg-opacity-50 z-50 flex items-center justify-center p-4 backdrop-blur-sm">
            <div
                class="bg-white rounded-xl shadow-2xl max-w-2xl w-full max-h-[90vh] overflow-y-auto transform transition-all border border-gray-100">
                <div class="flex justify-between items-center p-6 border-b border-gray-100 bg-gray-50/50 rounded-t-xl">
                    <h3 class="text-lg font-bold text-gray-900 flex items-center gap-2">
                        <i class="fas fa-info-circle text-blue-500"></i>
                        Detail Template
                    </h3>
                    <button onclick="closeModal()"
                        class="text-gray-400 hover:text-gray-600 transition-colors w-8 h-8 flex items-center justify-center rounded-full hover:bg-gray-100">
                        <i class="fas fa-times text-lg"></i>
                    </button>
                </div>
                <div id="modalContent" class="p-6"></div>
            </div>
        </div>

        <script>
            function applyFilters() {
                const type = document.getElementById('filterType').value;
                const channel = document.getElementById('filterChannel').value;
                const active = document.getElementById('filterActive').value;

                const params = new URLSearchParams();
                if (type) params.append('notification_type', type);
                if (channel) params.append('channel', channel);
                if (active) params.append('is_active', active);

                window.location.href = '/notification/templates?' + params.toString();
            }

            function resetFilters() {
                window.location.href = '/notification/templates';
            }

            async function toggleActive(code, newStatus) {
                if (!confirm(`Yakin ingin ${newStatus ? 'mengaktifkan' : 'menonaktifkan'} template ini?`)) return;

                try {
                    const response = await fetch(`/api/notification/templates/${code}`, {
                        method: 'PUT',
                        headers: { 'Content-Type': 'application/json' },
                        body: JSON.stringify({ is_active: newStatus })
                    });

                    const data = await response.json();
                    if (data.success) {
                        location.reload();
                    } else {
                        alert('Error: ' + data.message);
                    }
                } catch (error) {
                    alert('Error: ' + error.message);
                }
            }

            async function deleteTemplate(code) {
                if (!confirm('Yakin ingin menghapus template ini? Tindakan ini tidak dapat dibatalkan.')) return;

                try {
                    const response = await fetch(`/api/notification/templates/${code}`, {
                        method: 'DELETE'
                    });

                    const data = await response.json();
                    if (data.success) {
                        location.reload();
                    } else {
                        alert('Error: ' + data.message);
                    }
                } catch (error) {
                    alert('Error: ' + error.message);
                }
            }

            async function viewTemplate(code) {
                try {
                    const response = await fetch(`/api/notification/api/templates/${code}`);
                    const data = await response.json();

                    if (data.success) {
                        const template = data.data;
                        const modalContent = document.getElementById('modalContent');
                        modalContent.innerHTML = `
                    <div class="space-y-6">
                        <div class="grid grid-cols-2 gap-6">
                            <div>
                                <label class="block text-xs font-medium text-gray-500 uppercase tracking-wider mb-1">Kode Template</label>
                                <code class="block bg-gray-100 px-3 py-2 rounded text-sm font-mono text-gray-800 border border-gray-200 select-all">${template.template_code}</code>
                            </div>
                            <div>
                                <label class="block text-xs font-medium text-gray-500 uppercase tracking-wider mb-1">Nama Template</label>
                                <p class="text-sm font-medium text-gray-900 mt-2">${template.template_name}</p>
                            </div>
                        </div>

                        <div>
                            <label class="block text-xs font-medium text-gray-500 uppercase tracking-wider mb-1">Subject / Title</label>
                            <p class="bg-gray-50 p-3 rounded-lg text-sm text-gray-800 border border-gray-200 shadow-sm">${template.title_template}</p>
                        </div>
                        
                        <div>
                            <label class="block text-xs font-medium text-gray-500 uppercase tracking-wider mb-1">Message Body</label>
                            <div class="bg-gray-50 p-4 rounded-lg border border-gray-200 shadow-sm max-h-60 overflow-y-auto">
                                <pre class="text-sm text-gray-800 whitespace-pre-wrap font-sans leading-relaxed">${template.message_template}</pre>
                            </div>
                        </div>

                        <div class="grid grid-cols-2 gap-6 p-4 bg-gray-50 rounded-lg border border-gray-100">
                            <div>
                                <label class="block text-xs font-medium text-gray-500 uppercase tracking-wider">Channel</label>
                                <span class="inline-flex mt-2 items-center px-2.5 py-0.5 rounded-full text-xs font-medium bg-blue-100 text-blue-800">
                                    <i class="fab fa-${template.channel === 'email' ? 'google' : template.channel} mr-1.5"></i> ${template.channel.toUpperCase()}
                                </span>
                            </div>
                            <div>
                                <label class="block text-xs font-medium text-gray-500 uppercase tracking-wider">Priority</label>
                                <span class="inline-flex mt-2 items-center px-2.5 py-0.5 rounded-full text-xs font-medium ${template.priority === 'high' ? 'bg-red-100 text-red-800' :
                                template.priority === 'normal' ? 'bg-blue-100 text-blue-800' : 'bg-gray-100 text-gray-800'
                            }">
                                    ${template.priority}
                                </span>
                            </div>
                        </div>

                        ${template.variables && template.variables.length > 0 ? `
                            <div>
                                <label class="block text-xs font-medium text-gray-500 uppercase tracking-wider mb-2">Variabel Tersedia</label>
                                <div class="flex flex-wrap gap-2">
                                    ${template.variables.map(v => `<code class="bg-purple-50 text-purple-700 border border-purple-100 px-2 py-1 rounded text-xs font-mono">{${v}}</code>`).join('')}
                                </div>
                            </div>
                        ` : ''}
                    </div>
                `;
                        document.getElementById('templateModal').classList.remove('hidden');
                    }
                } catch (error) {
                    alert('Error: ' + error.message);
                }
            }

            function closeModal() {
                document.getElementById('templateModal').classList.add('hidden');
            }

            // Close on escape
            document.addEventListener('keydown', function (event) {
                if (event.key === "Escape") {
                    closeModal();
                }
            });

            // Close on click outside
            document.getElementById('templateModal').addEventListener('click', function (event) {
                if (event.target === this) {
                    closeModal();
                }
            });
        </script>
</body>

</html>