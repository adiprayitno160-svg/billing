<!DOCTYPE html>
<html lang="id">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= typeof title !== 'undefined' ? title : 'Error' %></title>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700;800&display=swap" rel="stylesheet">
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css" rel="stylesheet">
    <style>
        body {
            font-family: 'Inter', ui-sans-serif, system-ui, -apple-system, 'Segoe UI', Roboto, 'Helvetica Neue', Arial, sans-serif;
            -webkit-font-smoothing: antialiased;
            -moz-osx-font-smoothing: grayscale;
        }
    </style>
</head>
<body class="bg-gradient-to-br from-gray-50 via-blue-50 to-indigo-50 min-h-screen">
    <div class="min-h-screen flex items-center justify-center px-4 py-12">
        <div class="max-w-2xl w-full">
            <!-- Error Icon -->
            <div class="text-center mb-8">
                <% if (typeof status !== 'undefined' && status === 404) { %>
                    <div class="inline-flex items-center justify-center w-32 h-32 rounded-full bg-gradient-to-br from-orange-100 to-red-100 mb-6 shadow-lg">
                        <i class="fas fa-exclamation-triangle text-6xl text-orange-500"></i>
                    </div>
                <% } else { %>
                    <div class="inline-flex items-center justify-center w-32 h-32 rounded-full bg-gradient-to-br from-red-100 to-pink-100 mb-6 shadow-lg">
                        <i class="fas fa-exclamation-circle text-6xl text-red-500"></i>
                    </div>
                <% } %>
            </div>

            <!-- Error Content Card -->
            <div class="bg-white rounded-2xl shadow-2xl border border-gray-200 overflow-hidden">
                <div class="bg-gradient-to-r <%= typeof status !== 'undefined' && status === 404 ? 'from-orange-500 to-red-500' : 'from-red-500 to-pink-500' %> px-6 py-4">
                    <h1 class="text-2xl font-bold text-white">
                        <% if (typeof status !== 'undefined' && status === 404) { %>
                            Halaman Tidak Ditemukan
                        <% } else { %>
                            Terjadi Kesalahan
                        <% } %>
                    </h1>
                </div>
                
                <div class="p-8">
                    <div class="mb-6">
                        <div class="inline-flex items-center gap-2 px-4 py-2 rounded-lg <%= typeof status !== 'undefined' && status === 404 ? 'bg-orange-50 text-orange-700' : 'bg-red-50 text-red-700' %> border <%= typeof status !== 'undefined' && status === 404 ? 'border-orange-200' : 'border-red-200' %>">
                            <span class="font-semibold">Status:</span>
                            <span class="font-bold text-lg"><%= typeof status !== 'undefined' ? status : '500' %></span>
                        </div>
                    </div>

                    <div class="mb-8">
                        <p class="text-gray-700 text-lg leading-relaxed">
                            <%= typeof message !== 'undefined' ? message : (typeof status !== 'undefined' && status === 404 ? 'Halaman yang Anda cari tidak ditemukan' : 'Terjadi kesalahan pada server') %>
                        </p>
                        <% if (typeof status !== 'undefined' && status === 404) { %>
                            <p class="text-gray-500 text-sm mt-3">
                                Mungkin halaman telah dihapus, URL salah, atau Anda tidak memiliki akses ke halaman ini.
                            </p>
                        <% } %>
                        <% if (typeof error !== 'undefined' && error) { %>
                            <div class="mt-4 p-4 bg-gray-100 rounded-lg border border-gray-300">
                                <p class="text-sm font-semibold text-gray-700 mb-2">Detail Error:</p>
                                <p class="text-xs text-gray-600 font-mono break-all"><%= error %></p>
                            </div>
                        <% } %>
                    </div>

                    <!-- Action Buttons -->
                    <div class="flex flex-col sm:flex-row gap-3">
                        <a href="/dashboard" class="flex-1 inline-flex items-center justify-center gap-2 px-6 py-3 bg-gradient-to-r from-blue-500 to-indigo-600 text-white font-semibold rounded-lg hover:from-blue-600 hover:to-indigo-700 transition-all duration-300 shadow-lg hover:shadow-xl transform hover:-translate-y-0.5">
                            <i class="fas fa-home"></i>
                            <span>Kembali ke Dashboard</span>
                        </a>
                        <a href="/customers/list" class="flex-1 inline-flex items-center justify-center gap-2 px-6 py-3 bg-white border-2 border-gray-300 text-gray-700 font-semibold rounded-lg hover:bg-gray-50 hover:border-gray-400 transition-all duration-300">
                            <i class="fas fa-users"></i>
                            <span>Daftar Pelanggan</span>
                        </a>
                        <button onclick="window.history.back()" class="flex-1 inline-flex items-center justify-center gap-2 px-6 py-3 bg-white border-2 border-gray-300 text-gray-700 font-semibold rounded-lg hover:bg-gray-50 hover:border-gray-400 transition-all duration-300">
                            <i class="fas fa-arrow-left"></i>
                            <span>Kembali</span>
                        </button>
                    </div>
                </div>
            </div>

            <!-- Help Text -->
            <div class="mt-6 text-center">
                <p class="text-gray-500 text-sm">
                    Jika masalah berlanjut, silakan hubungi administrator sistem.
                </p>
            </div>
        </div>
    </div>
</body>
</html>


