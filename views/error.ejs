<!DOCTYPE html>
<html lang="id">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= typeof title !== 'undefined' ? title : 'Error' %></title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-100">
    <div class="max-w-xl mx-auto mt-16">
        <div class="rounded-lg border border-red-200 bg-red-50 p-6">
            <div class="text-lg font-semibold text-red-700">Terjadi Kesalahan</div>
            <div class="mt-2 text-red-800">Status: <%= typeof status !== 'undefined' ? status : '500' %></div>
            <div class="mt-1 text-red-800"><%= typeof message !== 'undefined' ? message : 'Internal Server Error' %></div>
        </div>
    </div>
</body>
</html>


