<div class="rounded border border-slate-200 bg-white p-4">
    <div class="flex items-center justify-between mb-4">
        <h2 class="text-lg font-semibold">Daftar ODP <%= odcId ? ('(ODC #' + odcId + ')' ) : '' %>
        </h2>
        <a href="/ftth/odp/add<%= odcId ? ('?odc_id=' + odcId) : '' %>"
            class="px-3 py-1.5 rounded bg-blue-600 text-white text-sm hover:bg-blue-700">Tambah ODP</a>
    </div>
    <div class="overflow-x-auto">
        <table class="min-w-full text-sm">
            <thead class="text-left text-slate-600 bg-slate-50/50">
                <tr>
                    <th class="py-2 px-3">Nama ODP</th>
                    <th class="py-2 px-3">Parent ODC</th>
                    <th class="py-2 px-3">Lokasi</th>
                    <th class="py-2 px-3">Port (T/U/S)</th>
                    <th class="py-2 px-3 text-right">Aksi</th>
                </tr>
            </thead>
            <tbody>
                <% (items || []).forEach(function(odp){ %>
                    <tr class="border-t border-slate-100 hover:bg-slate-50/50 transition-colors">
                        <td class="py-2 px-3 font-medium text-slate-700">
                            <%= odp.name %>
                        </td>
                        <td class="py-2 px-3 text-slate-500">
                            <span class="px-2 py-0.5 rounded bg-slate-100 text-slate-600 text-xs">
                                <%= odp.odc_name || ('#' + odp.odc_id) %>
                            </span>
                        </td>
                        <td class="py-2 px-3 text-slate-500 text-xs">
                            <%= odp.location || '-' %>
                        </td>
                        <td class="py-2 px-3">
                            <span class="font-semibold text-blue-600">
                                <%= odp.total_ports %>
                            </span> /
                            <span class="font-semibold text-orange-600">
                                <%= odp.used_ports %>
                            </span> /
                            <span class="font-semibold text-green-600">
                                <%= (odp.total_ports - odp.used_ports) %>
                            </span>
                        </td>
                        <td class="py-2 px-3 text-right">
                            <div class="flex justify-end gap-2 text-xs">
                                <a href="/ftth/odp/<%= odp.id %>"
                                    class="px-2 py-1 rounded border border-slate-300 text-slate-600 hover:bg-white">Edit</a>
                                <form method="post" action="/ftth/odp/<%= odp.id %>/delete"
                                    onsubmit="return confirm('Hapus ODP ini?');" class="inline">
                                    <button type="submit"
                                        class="px-2 py-1 rounded border border-rose-200 text-rose-600 hover:bg-rose-50">Hapus</button>
                                </form>
                            </div>
                        </td>
                    </tr>
                    <% }) %>
                        <% if (!items || items.length===0) { %>
                            <tr>
                                <td colspan="5" class="py-6 text-center text-slate-500">Belum ada data ODP</td>
                            </tr>
                            <% } %>
            </tbody>
        </table>
    </div>
</div>

<!-- Dialihkan ke halaman tambah khusus: /ftth/odp/add -->