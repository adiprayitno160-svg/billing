<div class="p-10 max-w-4xl mx-auto bg-white shadow mt-10 rounded">
    <h1 class="text-2xl font-bold mb-4 text-red-600">DEBUG MODE: ODC EDIT</h1>
    <p class="mb-4 text-gray-600">Jika Anda melihat halaman ini, berarti Controller dan Route berfungsi normal.</p>

    <div class="bg-gray-100 p-4 rounded mb-6 border font-mono text-xs overflow-auto max-h-64">
        <strong>Data Item (Dump):</strong><br>
        <%= JSON.stringify(item, null, 2) %>
    </div>

    <div class="bg-blue-50 p-4 rounded mb-6 border font-mono text-xs overflow-auto max-h-40">
        <strong>Data OLT (Dump - First 2):</strong><br>
        <%= JSON.stringify((olts || []).slice(0, 2), null, 2) %>
    </div>

    <form method="post" action="/ftth/odc/<%= item.id %>" class="space-y-6 border-t pt-6">
        <div>
            <label class="block font-bold mb-1">Nama ODC</label>
            <input type="text" name="name" value="<%= item.name %>" class="border p-2 w-full rounded">
        </div>

        <div>
            <label class="block font-bold mb-1">OLT Uplink</label>
            <select name="olt_id" class="border p-2 w-full rounded">
                <% (olts || []).forEach(o=> { %>
                    <option value="<%= o.id %>" <%=item.olt_id==o.id ? 'selected' : '' %>><%= o.name %>
                    </option>
                    <% }) %>
            </select>
        </div>

        <div>
            <label class="block font-bold mb-1">Total Port</label>
            <input type="number" name="total_ports" value="<%= item.total_ports %>" class="border p-2 w-full rounded">
        </div>

        <!-- Hidden fields for compatibility -->
        <input type="hidden" name="latitude" value="<%= item.latitude %>">
        <input type="hidden" name="longitude" value="<%= item.longitude %>">

        <div class="flex gap-4">
            <a href="/ftth/odc" class="bg-gray-500 text-white px-6 py-3 rounded font-bold">KEMBALI</a>
            <button type="submit" class="bg-red-600 text-white px-6 py-3 rounded font-bold hover:bg-red-700">SIMPAN
                PERUBAHAN (DEBUG)</button>
        </div>
    </form>
</div>