<div class="space-y-8 animate-fadeIn">
    <!-- Hero Section: Stats & Greeting -->
    <div class="grid grid-cols-1 lg:grid-cols-4 gap-6">
        <div class="lg:col-span-2 flex flex-col justify-center">
            <h1 class="text-4xl font-black text-slate-800 tracking-tight mb-2">WhatsApp Business API 🚀</h1>
            <p class="text-slate-500 font-medium">Platform notifikasi otomatis dan layanan pelanggan mandiri.</p>
            <div class="flex gap-3 mt-6">
                <div
                    class="px-4 py-2 bg-green-100 text-green-700 rounded-2xl text-xs font-black flex items-center gap-2 border border-green-200">
                    <span class="w-2 h-2 bg-green-500 rounded-full animate-pulse"></span>
                    SISTEM OPERASIONAL
                </div>
                <div class="px-4 py-2 bg-blue-100 text-blue-700 rounded-2xl text-xs font-black border border-blue-200">
                    PROVIDER: <%= status.provider || 'WhatsApp Web' %>
                </div>
            </div>
        </div>

        <!-- Quick Stats Cards -->
        <div
            class="bg-white p-6 rounded-[2rem] border border-slate-100 shadow-xl shadow-slate-200/50 flex flex-col justify-between group hover:-translate-y-1 transition-all">
            <div class="flex justify-between items-start">
                <div
                    class="w-12 h-12 bg-indigo-50 text-indigo-600 rounded-2xl flex items-center justify-center text-xl group-hover:scale-110 transition-transform">
                    <i class="fas fa-paper-plane"></i>
                </div>
                <span class="text-[10px] font-black text-slate-400 uppercase tracking-widest">Outbound</span>
            </div>
            <div>
                <h4 class="text-3xl font-black text-slate-800 tracking-tighter">
                    <%= stats.sent %>
                </h4>
                <p class="text-xs text-slate-500 font-bold">Pesan Terkirim</p>
            </div>
        </div>

        <div
            class="bg-white p-6 rounded-[2rem] border border-slate-100 shadow-xl shadow-slate-200/50 flex flex-col justify-between group hover:-translate-y-1 transition-all">
            <div class="flex justify-between items-start">
                <div
                    class="w-12 h-12 bg-emerald-50 text-emerald-600 rounded-2xl flex items-center justify-center text-xl group-hover:scale-110 transition-transform">
                    <i class="fas fa-comment-medical"></i>
                </div>
                <span class="text-[10px] font-black text-slate-400 uppercase tracking-widest">Inbound</span>
            </div>
            <div>
                <h4 class="text-3xl font-black text-slate-800 tracking-tighter">
                    <%= stats.received %>
                </h4>
                <p class="text-xs text-slate-500 font-bold">Pesan Masuk</p>
            </div>
        </div>
    </div>

    <!-- Main Features Grid -->
    <div class="grid grid-cols-1 lg:grid-cols-3 gap-8">

        <!-- Feature 1: Connection & Sync -->
        <div
            class="bg-white rounded-[2.5rem] shadow-2xl shadow-slate-200/60 border border-slate-50 overflow-hidden group">
            <div class="p-8 space-y-6">
                <div class="flex items-center gap-4">
                    <div
                        class="w-14 h-14 bg-slate-900 rounded-2xl flex items-center justify-center shadow-lg group-hover:rotate-6 transition-transform">
                        <i class="fab fa-whatsapp text-2xl text-green-400"></i>
                    </div>
                    <div>
                        <h3 class="text-xl font-black text-slate-800">Koneksi Device</h3>
                        <p class="text-xs text-slate-500 font-bold uppercase tracking-wider">Authentication Core</p>
                    </div>
                </div>

                <!-- Connection State Wrapper -->
                <div id="connection-status-area"
                    class="bg-slate-50 rounded-[2rem] p-6 border border-slate-100 relative min-h-[300px] flex flex-col items-center justify-center text-center">
                    <% if (status.ready) { %>
                        <!-- Connected View -->
                        <div class="animate-in zoom-in duration-500">
                            <div
                                class="w-20 h-20 bg-green-100 text-green-600 rounded-full flex items-center justify-center text-3xl mb-4 mx-auto shadow-inner">
                                <i class="fas fa-check-double"></i>
                            </div>
                            <h4 class="text-lg font-black text-slate-800">Device Terhubung!</h4>
                            <p class="text-xs text-slate-500 px-4 mt-2">Sistem sinkronisasi real-time dengan aplikasi
                                WhatsApp Anda sedang berjalan aktif.</p>

                            <div class="mt-8 pt-6 border-t border-slate-200 w-full">
                                <p class="text-[10px] font-black text-slate-400 uppercase mb-4 tracking-widest">Master
                                    Controls</p>
                                <div class="grid grid-cols-2 gap-3">
                                    <button onclick="clearSession()"
                                        class="px-4 py-2 bg-white border border-red-100 text-red-600 rounded-xl text-[10px] font-black hover:bg-red-50 transition-all active:scale-95 shadow-sm">
                                        PUTUS KONEKSI
                                    </button>
                                    <button onclick="regenerateQRCode()"
                                        class="px-4 py-2 bg-slate-900 text-white rounded-xl text-[10px] font-black hover:bg-slate-800 transition-all active:scale-95 shadow-lg">
                                        RE-GENERATE QR
                                    </button>
                                </div>
                            </div>
                        </div>
                        <% } else { %>
                            <!-- Disconnected View / QR Area -->
                            <div id="qr-container" class="space-y-4 animate-in fade-in duration-700">
                                <!-- Loading State -->
                                <div id="qrCodeLoading" class="hidden">
                                    <div
                                        class="w-12 h-12 border-4 border-slate-200 border-t-blue-600 rounded-full animate-spin mx-auto mb-4">
                                    </div>
                                    <p class="text-[10px] font-black text-slate-400 uppercase tracking-widest">
                                        Generating Secure QR...</p>
                                </div>

                                <!-- Image Display -->
                                <div id="qrCodeDisplay" class="<%= (qrCode && qrCodeUrl) ? '' : 'hidden' %>">
                                    <div
                                        class="p-3 bg-white rounded-3xl shadow-xl border border-slate-100 inline-block group/qr relative overflow-hidden">
                                        <div
                                            class="absolute inset-0 bg-blue-600/0 group-hover/qr:bg-blue-600/5 transition-all">
                                        </div>
                                        <img id="qrCodeImage"
                                            src="<%= qrCodeUrl ? qrCodeUrl + '?t=' + Date.now() : '' %>" alt="QR Code"
                                            class="w-48 h-48 rounded-2xl relative z-10">
                                    </div>
                                    <div class="mt-6">
                                        <h4 class="text-lg font-black text-slate-800">Scan QR Code</h4>
                                        <p
                                            class="text-[10px] text-slate-500 mt-1 font-medium bg-blue-50 py-1 px-3 rounded-full inline-block">
                                            WhatsApp > Linked Devices > Link a Device</p>
                                    </div>
                                </div>

                                <!-- Empty State -->
                                <div id="qrCodeEmpty" class="<%= (!qrCode || !qrCodeUrl) ? '' : 'hidden' %> opacity-30">
                                    <i class="fas fa-qrcode text-6xl mb-4"></i>
                                    <p class="text-xs font-bold uppercase tracking-widest">Waiting for Signal...</p>
                                </div>

                                <button id="qr-gen-btn" onclick="regenerateQRCode()"
                                    class="w-full mt-4 bg-blue-600 text-white py-3.5 rounded-2xl font-black shadow-lg shadow-blue-500/30 hover:shadow-blue-500/50 transition-all active:scale-95 flex items-center justify-center gap-2">
                                    <i class="fas fa-sync-alt"></i>
                                    AKTIFKAN QR CODE
                                </button>
                            </div>
                            <% } %>
                </div>
            </div>
        </div>

        <!-- Feature 2: Core Utilities -->
        <div class="lg:col-span-2 grid grid-cols-1 md:grid-cols-2 gap-6">

            <!-- Broadcast Card -->
            <a href="/whatsapp/broadcast"
                class="group bg-white p-8 rounded-[2.5rem] border border-slate-100 shadow-xl shadow-slate-200/40 flex flex-col justify-between hover:shadow-indigo-500/10 hover:-translate-y-2 transition-all">
                <div class="flex justify-between items-start mb-12">
                    <div
                        class="w-16 h-16 bg-indigo-600 text-white rounded-2xl flex items-center justify-center text-2xl shadow-lg group-hover:scale-110 transition-transform">
                        <i class="fas fa-bullhorn rotate-[-12deg]"></i>
                    </div>
                    <div
                        class="px-3 py-1 bg-indigo-50 text-indigo-600 rounded-full text-[9px] font-black uppercase tracking-widest">
                        Campaigns</div>
                </div>
                <div>
                    <h3 class="text-2xl font-black text-slate-800 mb-2">Broadcast Massal</h3>
                    <p class="text-xs text-slate-500 font-medium leading-relaxed">Kirim pengumuman, tagihan, atau promo
                        sekali klik ke ribuan pelanggan dengan personalisasi variabel otomatis.</p>
                    <div class="mt-6 flex items-center gap-2 text-indigo-600 font-black text-xs">
                        MULAI BROADCAST <i
                            class="fas fa-arrow-right group-hover:translate-x-2 transition-transform"></i>
                    </div>
                </div>
            </a>

            <!-- QRIS Card -->
            <a href="/whatsapp/qris"
                class="group bg-white p-8 rounded-[2.5rem] border border-slate-100 shadow-xl shadow-slate-200/40 flex flex-col justify-between hover:shadow-blue-500/10 hover:-translate-y-2 transition-all">
                <div class="flex justify-between items-start mb-12">
                    <div
                        class="w-16 h-16 bg-blue-500 text-white rounded-2xl flex items-center justify-center text-2xl shadow-lg group-hover:scale-110 transition-transform">
                        <i class="fas fa-qrcode"></i>
                    </div>
                    <div
                        class="px-3 py-1 bg-blue-50 text-blue-600 rounded-full text-[9px] font-black uppercase tracking-widest">
                        Payments</div>
                </div>
                <div>
                    <h3 class="text-2xl font-black text-slate-800 mb-2">Kirim QRIS Instan</h3>
                    <p class="text-xs text-slate-500 font-medium leading-relaxed">Memudahkan pembayaran pelanggan dengan
                        mengirimkan gambar QRIS statis atau dinamis langsung ke chat pribadi mereka.</p>
                    <div class="mt-6 flex items-center gap-2 text-blue-600 font-black text-xs">
                        KIRIM TAGIHAN <i class="fas fa-arrow-right group-hover:translate-x-2 transition-transform"></i>
                    </div>
                </div>
            </a>

            <!-- Monitor Card -->
            <a href="/settings/whatsapp/monitor"
                class="group bg-slate-900 p-8 rounded-[2.5rem] shadow-2xl shadow-slate-900/40 flex flex-col justify-between hover:-translate-y-2 transition-all relative overflow-hidden col-span-1 md:col-span-2">
                <div class="absolute -right-10 -bottom-10 w-40 h-40 bg-blue-600 rounded-full blur-[80px] opacity-20">
                </div>
                <div class="flex justify-between items-start mb-8 relative z-10">
                    <div
                        class="w-16 h-16 bg-white/10 text-white rounded-2xl flex items-center justify-center text-2xl backdrop-blur-md border border-white/20 group-hover:scale-110 transition-transform">
                        <i class="fas fa-desktop"></i>
                    </div>
                    <div
                        class="px-3 py-1 bg-emerald-500/20 text-emerald-400 rounded-full text-[9px] font-black uppercase tracking-widest border border-emerald-500/30">
                        Analytics & Ops</div>
                </div>
                <div class="relative z-10">
                    <h3 class="text-2xl font-black text-white mb-2">Control Center & Verifikasi</h3>
                    <p class="text-xs text-slate-400 font-medium leading-relaxed max-w-lg">Lihat log pesan masuk/keluar
                        secara real-time. Bagian terpenting adalah verifikasi bukti transfer manual yang dikirim
                        pelanggan via WhatsApp.</p>
                    <div class="mt-8 flex flex-wrap gap-4">
                        <div class="px-4 py-2 bg-white/5 rounded-xl border border-white/10 flex items-center gap-2">
                            <span class="w-2 h-2 bg-emerald-500 rounded-full"></span>
                            <span class="text-[10px] font-bold text-white uppercase tracking-wider">Live
                                Monitoring</span>
                        </div>
                        <div class="px-4 py-2 bg-white/5 rounded-xl border border-white/10 flex items-center gap-2">
                            <span class="w-2 h-2 bg-amber-500 rounded-full"></span>
                            <span class="text-[10px] font-bold text-white uppercase tracking-wider">Manual
                                Approval</span>
                        </div>
                    </div>
                </div>
            </a>
        </div>
    </div>

    <!-- Bottom Context: Recent History & Status -->
    <div class="grid grid-cols-1 lg:grid-cols-3 gap-8 pb-12">

        <!-- Recent History Section -->
        <div
            class="lg:col-span-2 bg-white rounded-[2.5rem] border border-slate-100 shadow-xl shadow-slate-200/40 overflow-hidden">
            <div class="p-8 border-b border-slate-50 flex items-center justify-between">
                <h3 class="text-xl font-black text-slate-800">Riwayat Notifikasi Terbaru</h3>
                <a href="/notification/history"
                    class="text-xs font-black text-blue-600 hover:underline tracking-tight">LIHAT SEMUA HISTORY</a>
            </div>
            <div class="overflow-x-auto">
                <table class="w-full">
                    <thead class="bg-slate-50/50">
                        <tr>
                            <th
                                class="px-8 py-4 text-left text-[10px] font-black text-slate-400 uppercase tracking-widest">
                                Penerima</th>
                            <th
                                class="px-8 py-4 text-left text-[10px] font-black text-slate-400 uppercase tracking-widest">
                                Tipe Pesan</th>
                            <th
                                class="px-8 py-4 text-left text-[10px] font-black text-slate-400 uppercase tracking-widest">
                                Waktu</th>
                            <th
                                class="px-8 py-4 text-center text-[10px] font-black text-slate-400 uppercase tracking-widest">
                                Status</th>
                        </tr>
                    </thead>
                    <tbody class="divide-y divide-slate-50">
                        <% if (history && history.length> 0) { %>
                            <% history.forEach(item=> { %>
                                <tr class="hover:bg-slate-50/50 transition-colors">
                                    <td class="px-8 py-5">
                                        <p class="text-sm font-black text-slate-800">
                                            <%= item.customer_name || 'System' %>
                                        </p>
                                        <p class="text-[10px] text-slate-400 font-mono tracking-tighter">
                                            <%= item.phone_number || '-' %>
                                        </p>
                                    </td>
                                    <td class="px-8 py-5">
                                        <span
                                            class="px-2.5 py-1 bg-slate-100 text-slate-600 rounded-lg text-[9px] font-black uppercase tracking-widest">
                                            <%= item.message_type || 'OUTBOUND' %>
                                        </span>
                                    </td>
                                    <td class="px-8 py-5">
                                        <p class="text-xs font-bold text-slate-600">
                                            <%= new Date(item.created_at).toLocaleString('id-ID', {day: 'numeric' ,
                                                month: 'short' , hour: '2-digit' , minute: '2-digit' }) %>
                                        </p>
                                    </td>
                                    <td class="px-8 py-5 text-center">
                                        <div class="flex items-center justify-center">
                                            <% if (item.status==='sent' ) { %>
                                                <div
                                                    class="w-8 h-8 rounded-full bg-green-100 text-green-600 flex items-center justify-center text-xs shadow-sm">
                                                    <i class="fas fa-check"></i></div>
                                                <% } else if (item.status==='failed' ) { %>
                                                    <div
                                                        class="w-8 h-8 rounded-full bg-red-100 text-red-600 flex items-center justify-center text-xs shadow-sm">
                                                        <i class="fas fa-times"></i></div>
                                                    <% } else { %>
                                                        <div
                                                            class="w-8 h-8 rounded-full bg-blue-100 text-blue-600 flex items-center justify-center text-xs shadow-sm">
                                                            <i class="fas fa-clock"></i></div>
                                                        <% } %>
                                        </div>
                                    </td>
                                </tr>
                                <% }) %>
                                    <% } else { %>
                                        <tr>
                                            <td colspan="4" class="px-8 py-20 text-center text-slate-300 italic">
                                                <i class="fas fa-inbox text-4xl mb-3 block"></i>
                                                Belum ada aktivitas terekam.
                                            </td>
                                        </tr>
                                        <% } %>
                    </tbody>
                </table>
            </div>
        </div>

        <!-- System Guard Section -->
        <div
            class="bg-gradient-to-br from-blue-600 via-indigo-700 to-slate-900 rounded-[2.5rem] p-8 text-white shadow-2xl relative overflow-hidden">
            <div class="absolute right-0 top-0 w-32 h-32 bg-white/10 rounded-full blur-3xl"></div>
            <div class="relative z-10 flex flex-col h-full">
                <div class="mb-8">
                    <h3 class="text-xl font-black mb-2">Automated Assistant</h3>
                    <p class="text-xs text-blue-200 font-medium">Layanan mandiri pelanggan (Self-Service) menggunakan
                        sistem respon otomatis.</p>
                </div>

                <div class="space-y-4 flex-1">
                    <div
                        class="p-4 bg-white/10 rounded-2xl border border-white/10 backdrop-blur-md group hover:bg-white/20 transition-all cursor-pointer">
                        <div class="flex items-start gap-4">
                            <div class="w-10 h-10 bg-indigo-500 rounded-xl flex items-center justify-center shadow-lg">
                                <i class="fas fa-robot text-sm"></i></div>
                            <div>
                                <p class="text-[10px] font-black text-indigo-300 uppercase tracking-widest">Self-Service
                                    Payment</p>
                                <p class="text-xs font-bold mt-1">Pelanggan bisa cek iuran sendiri dengan kirim pesan
                                    "TAGIHAN"</p>
                            </div>
                        </div>
                    </div>

                    <div
                        class="p-4 bg-white/10 rounded-2xl border border-white/10 backdrop-blur-md group hover:bg-white/20 transition-all cursor-pointer">
                        <div class="flex items-start gap-4">
                            <div class="w-10 h-10 bg-emerald-500 rounded-xl flex items-center justify-center shadow-lg">
                                <i class="fas fa-shield-alt text-sm"></i></div>
                            <div>
                                <p class="text-[10px] font-black text-emerald-300 uppercase tracking-widest">Automatic
                                    Isolation</p>
                                <p class="text-xs font-bold mt-1">Kirim notifikasi isolir otomatis tepat saat sistem
                                    MikroTik memutus akses.</p>
                            </div>
                        </div>
                    </div>
                </div>

                <div class="mt-8 pt-6 border-t border-white/10">
                    <a href="/settings/system"
                        class="w-full bg-white text-slate-900 py-3 rounded-xl font-black text-xs text-center shadow-xl hover:bg-slate-100 transition-all block">PENGATURAN
                        SISTEM</a>
                </div>
            </div>
        </div>
    </div>
</div>

<!-- Scripts Specific to Dashboard -->
<script>
    let statusCheckInterval = null;
    let lastSeenQR = '';
    let isRegenerating = false;

    function startStatusCheck() {
        const isReady = <%= status.ready ? 'true' : 'false' %>;
        if (!isReady) {
            statusCheckInterval = setInterval(refreshStatus, 3000);
        }
    }

    startStatusCheck();

    async function refreshStatus() {
        if (isRegenerating) return;

        try {
            const response = await fetch('/whatsapp/status?t=' + Date.now());
            if (!response.ok) return;
            const result = await response.json();

            if (result.success) {
                const data = result.data;
                const currentStatus = data.status || data;

                if (currentStatus.ready) {
                    clearInterval(statusCheckInterval);
                    window.location.reload();
                    return;
                }

                const img = document.getElementById('qrCodeImage');
                if (data.qrCodeUrl && data.qrCode && img) {
                    if (data.qrCode !== lastSeenQR) {
                        lastSeenQR = data.qrCode;
                        console.log('New QR Code detected, updating image...');
                        document.getElementById('qrCodeLoading')?.classList.add('hidden');
                        document.getElementById('qrCodeEmpty')?.classList.add('hidden');
                        document.getElementById('qrCodeDisplay')?.classList.remove('hidden');
                        img.src = data.qrCodeUrl + '?t=' + Date.now();
                    }
                }
            }
        } catch (error) {
            console.error('Status refresh error:', error);
        }
    }

    async function clearSession() {
        if (!confirm('Session akan dihapus. Lanjutkan?')) return;

        const btn = event.target;
        const originalText = btn.innerText;
        btn.disabled = true;
        btn.innerText = 'WAIT...';

        try {
            const response = await fetch('/whatsapp/clear-session', { method: 'POST' });
            const result = await response.json();
            if (result.success) {
                window.location.reload();
            } else {
                alert('Gagal: ' + (result.error || 'Unknown error'));
            }
        } catch (e) {
            alert('Error menghapus session');
        } finally {
            btn.disabled = false;
            btn.innerText = originalText;
        }
    }

    async function regenerateQRCode() {
        isRegenerating = true;
        const btn = document.getElementById('qr-gen-btn');
        if (btn) {
            btn.disabled = true;
            btn.innerHTML = '<i class="fas fa-spinner fa-spin"></i> GENERATING...';
        }

        document.getElementById('qrCodeLoading')?.classList.remove('hidden');
        document.getElementById('qrCodeDisplay')?.classList.add('hidden');
        document.getElementById('qrCodeEmpty')?.classList.add('hidden');

        try {
            const response = await fetch('/whatsapp/regenerate-qr', { method: 'POST' });
            const result = await response.json();

            isRegenerating = false;
            if (result.success) {
                // refreshStatus will pick it up
            } else {
                alert('Gagal generate QR: ' + (result.error || 'Unknown error'));
                document.getElementById('qrCodeLoading')?.classList.add('hidden');
                document.getElementById('qrCodeEmpty')?.classList.remove('hidden');
                if (btn) {
                    btn.disabled = false;
                    btn.innerHTML = '<i class="fas fa-sync-alt"></i> AKTIFKAN QR CODE';
                }
            }
        } catch (error) {
            isRegenerating = false;
            alert('Error generating QR Code');
            document.getElementById('qrCodeLoading')?.classList.add('hidden');
            document.getElementById('qrCodeEmpty')?.classList.remove('hidden');
            if (btn) {
                btn.disabled = false;
                btn.innerHTML = '<i class="fas fa-sync-alt"></i> AKTIFKAN QR CODE';
            }
        }
    }
</script>