<!DOCTYPE html>
<html lang="id">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= title %> - Portal Prepaid</title>
  <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
  <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
  <style>
    /* Ensure icons and text are visible */
    .fas, .fab, .far {
      display: inline-block !important;
      visibility: visible !important;
      opacity: 1 !important;
    }
    i[class*="fa-"] {
      color: inherit !important;
    }
    label, p, span, h1, h2, h3, h4, h5, h6 {
      color: inherit !important;
    }
  </style>
</head>
<body class="bg-gradient-to-br from-blue-50 to-indigo-100 min-h-screen">
  <!-- Header -->
  <div class="bg-white shadow-sm">
    <div class="container mx-auto px-4 py-4">
      <h1 class="text-2xl font-bold text-indigo-600">
        <i class="fas fa-wifi mr-2"></i>Portal Prepaid
      </h1>
    </div>
  </div>

  <!-- Main Content -->
  <div class="container mx-auto px-4 py-8">
    <div class="max-w-4xl mx-auto">
      <!-- Title -->
      <div class="text-center mb-8">
        <h2 class="text-3xl font-bold text-gray-800 mb-2">
          💳 Pilih Metode Pembayaran
        </h2>
        <p class="text-gray-600">Bayar paket <strong><%= package.name %></strong></p>
        <p class="text-2xl font-bold text-indigo-600 mt-2">
          Rp <%= Number(package.price).toLocaleString('id-ID') %>
        </p>
      </div>

      <!-- Payment Methods -->
      <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
        <!-- Manual Transfer -->
        <% if (paymentSettings.bank_transfer_enabled) { %>
          <div class="bg-white rounded-xl shadow-lg overflow-hidden transform transition hover:scale-105 hover:shadow-2xl cursor-pointer"
               onclick="selectMethod('transfer')">
            <div class="bg-gradient-to-r from-green-500 to-teal-600 p-6 text-white">
              <div class="text-center">
                <i class="fas fa-university text-6xl mb-4"></i>
                <h3 class="text-2xl font-bold">Transfer Bank</h3>
              </div>
            </div>
            <div class="p-6">
              <div class="space-y-3 mb-6">
                <div class="flex items-center text-gray-700">
                  <i class="fas fa-check-circle text-green-600 mr-2"></i>
                  Upload bukti transfer
                </div>
                <div class="flex items-center text-gray-700">
                  <i class="fas fa-clock text-yellow-600 mr-2"></i>
                  Verifikasi manual (1-24 jam)
                </div>
                <div class="flex items-center text-gray-700">
                  <i class="fas fa-money-bill-wave text-blue-600 mr-2"></i>
                  <%= paymentSettings.bank_name %> - <%= paymentSettings.bank_account_number %>
                </div>
              </div>
              <button type="button" onclick="selectMethod('transfer')"
                      class="w-full bg-green-600 hover:bg-green-700 text-white font-bold py-3 px-4 rounded-lg">
                <i class="fas fa-university mr-2"></i>Pilih Transfer Bank
              </button>
            </div>
          </div>
        <% } %>

        <!-- Payment Gateway -->
        <% if (paymentSettings.payment_gateway_enabled) { %>
          <div class="bg-white rounded-xl shadow-lg overflow-hidden transform transition hover:scale-105 hover:shadow-2xl cursor-pointer"
               onclick="selectMethod('gateway')">
            <div class="bg-gradient-to-r from-purple-500 to-pink-600 p-6 text-white">
              <div class="text-center">
                <i class="fas fa-credit-card text-6xl mb-4"></i>
                <h3 class="text-2xl font-bold">Payment Gateway</h3>
              </div>
            </div>
            <div class="p-6">
              <div class="space-y-3 mb-6">
                <div class="flex items-center text-gray-700">
                  <i class="fas fa-bolt text-yellow-600 mr-2"></i>
                  Aktif otomatis & instan
                </div>
                <div class="flex items-center text-gray-700">
                  <i class="fas fa-shield-alt text-green-600 mr-2"></i>
                  Aman & terpercaya
                </div>
                <div class="flex items-center text-gray-700">
                  <i class="fas fa-wallet text-blue-600 mr-2"></i>
                  VA / QRIS / eWallet / CC
                </div>
              </div>
              <button type="button" onclick="selectMethod('gateway')"
                      class="w-full bg-purple-600 hover:bg-purple-700 text-white font-bold py-3 px-4 rounded-lg">
                <i class="fas fa-credit-card mr-2"></i>Pilih Payment Gateway
              </button>
            </div>
          </div>
        <% } %>

        <% if (!paymentSettings.bank_transfer_enabled && !paymentSettings.payment_gateway_enabled) { %>
          <div class="col-span-2 bg-white rounded-lg shadow p-12 text-center">
            <i class="fas fa-exclamation-circle text-6xl text-yellow-400 mb-4"></i>
            <p class="text-gray-600 text-lg">Metode pembayaran belum tersedia</p>
            <p class="text-gray-500 mt-2">Hubungi admin untuk informasi lebih lanjut</p>
          </div>
        <% } %>
      </div>

      <!-- Info -->
      <div class="mt-8 bg-blue-50 border-l-4 border-blue-500 p-4 rounded">
        <div class="flex">
          <div class="flex-shrink-0">
            <i class="fas fa-info-circle text-blue-500 text-xl"></i>
          </div>
          <div class="ml-3">
            <p class="text-sm text-blue-700">
              <strong>Informasi:</strong> Pilih metode pembayaran yang Anda inginkan.
              Transfer bank memerlukan verifikasi manual oleh admin (1-24 jam).
              Payment gateway akan mengaktifkan paket secara otomatis dan instan.
            </p>
          </div>
        </div>
      </div>

      <!-- Forms (Hidden) -->
      <form id="transferForm" action="/prepaid/portal/payment/manual-transfer" method="POST" style="display: none;">
        <input type="hidden" name="package_id" value="<%= package.id %>">
      </form>

      <form id="gatewayForm" action="/prepaid/portal/payment/gateway" method="POST" style="display: none;">
        <input type="hidden" name="package_id" value="<%= package.id %>">
        <input type="hidden" name="gateway" value="<%= paymentSettings.payment_gateway_provider %>">
      </form>
    </div>
  </div>

  <script>
    function selectMethod(method) {
      if (method === 'transfer') {
        window.location.href = '/prepaid/portal/payment/transfer?package_id=<%= package.id %>';
      } else if (method === 'gateway') {
        document.getElementById('gatewayForm').submit();
      }
    }
  </script>
</body>
</html>

