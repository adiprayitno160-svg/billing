<!DOCTYPE html>
<html lang="id">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <script>
        // Auto redirect after 10 seconds
        setTimeout(function() {
            const redirect = document.getElementById('autoRedirect');
            if (redirect && !redirect.checked) {
                // window.location.href = '/prepaid/portal/packages';
            }
        }, 10000);
    </script>
</head>
<body class="bg-gray-50">
    <div class="min-h-screen flex items-center justify-center p-4">
        <div class="max-w-2xl w-full">
            <!-- Success Card -->
            <div class="bg-white rounded-2xl shadow-2xl overflow-hidden">
                <!-- Success Header -->
                <div class="bg-gradient-to-r from-green-500 to-emerald-600 p-12 text-center">
                    <div class="w-24 h-24 bg-white rounded-full mx-auto mb-6 flex items-center justify-center animate-bounce">
                        <i class="fas fa-check text-5xl text-green-600"></i>
                    </div>
                    <h1 class="text-3xl font-bold text-white mb-2">Pembayaran Berhasil!</h1>
                    <p class="text-green-100 text-lg">Internet Anda sudah aktif</p>
                </div>

                <!-- Success Body -->
                <div class="p-8">
                    <!-- Package Info -->
                    <% if (subscription) { %>
                    <div class="bg-gradient-to-r from-blue-50 to-purple-50 rounded-xl p-6 mb-6">
                        <h3 class="font-bold text-lg text-gray-800 mb-4 flex items-center">
                            <i class="fas fa-box text-blue-600 mr-2"></i>
                            Detail Paket Anda
                        </h3>
                        <div class="grid grid-cols-1 md:grid-cols-2 gap-4">
                            <div>
                                <p class="text-sm text-gray-600 mb-1">Paket</p>
                                <p class="font-semibold text-gray-800"><%= subscription.package_name %></p>
                            </div>
                            <div>
                                <p class="text-sm text-gray-600 mb-1">Kecepatan</p>
                                <p class="font-semibold text-gray-800">
                                    <%= subscription.download_mbps %>/<%= subscription.upload_mbps %> Mbps
                                </p>
                            </div>
                            <div>
                                <p class="text-sm text-gray-600 mb-1">Mulai Aktif</p>
                                <p class="font-semibold text-gray-800">
                                    <%= new Date(subscription.activation_date).toLocaleString('id-ID') %>
                                </p>
                            </div>
                            <div>
                                <p class="text-sm text-gray-600 mb-1">Berakhir Pada</p>
                                <p class="font-semibold text-gray-800">
                                    <%= new Date(subscription.expiry_date).toLocaleString('id-ID') %>
                                </p>
                            </div>
                        </div>
                    </div>
                    <% } %>

                    <!-- Invoice Info -->
                    <% if (invoice) { %>
                    <div class="bg-gray-50 rounded-xl p-6 mb-6">
                        <h3 class="font-bold text-lg text-gray-800 mb-4 flex items-center">
                            <i class="fas fa-receipt text-purple-600 mr-2"></i>
                            Detail Pembayaran
                        </h3>
                        <div class="space-y-3">
                            <div class="flex justify-between items-center">
                                <span class="text-gray-600">No. Invoice</span>
                                <span class="font-semibold text-gray-800"><%= invoice.invoice_number %></span>
                            </div>
                            <div class="flex justify-between items-center">
                                <span class="text-gray-600">Tanggal Bayar</span>
                                <span class="font-semibold text-gray-800">
                                    <%= new Date().toLocaleDateString('id-ID') %>
                                </span>
                            </div>
                            <div class="flex justify-between items-center">
                                <span class="text-gray-600">Total Bayar</span>
                                <span class="font-semibold text-gray-800">
                                    Rp <%= invoice.total_amount.toLocaleString('id-ID') %>
                                </span>
                            </div>
                            <div class="flex justify-between items-center pt-3 border-t">
                                <span class="text-gray-600">Status</span>
                                <span class="bg-green-100 text-green-700 px-3 py-1 rounded-full text-sm font-semibold">
                                    <i class="fas fa-check-circle mr-1"></i>Lunas
                                </span>
                            </div>
                        </div>
                    </div>
                    <% } %>

                    <!-- Next Steps -->
                    <div class="bg-blue-50 rounded-xl p-6 mb-6">
                        <h3 class="font-bold text-lg text-blue-900 mb-4 flex items-center">
                            <i class="fas fa-lightbulb text-yellow-500 mr-2"></i>
                            Langkah Selanjutnya
                        </h3>
                        <ul class="space-y-3 text-sm text-blue-800">
                            <li class="flex items-start">
                                <i class="fas fa-check-circle text-blue-600 mr-3 mt-1"></i>
                                <span>Internet Anda sudah otomatis aktif dan siap digunakan</span>
                            </li>
                            <li class="flex items-start">
                                <i class="fas fa-check-circle text-blue-600 mr-3 mt-1"></i>
                                <span>Silakan refresh/restart browser atau sambungkan ulang WiFi</span>
                            </li>
                            <li class="flex items-start">
                                <i class="fas fa-check-circle text-blue-600 mr-3 mt-1"></i>
                                <span>Detail paket dan invoice dikirim ke WhatsApp Anda</span>
                            </li>
                            <li class="flex items-start">
                                <i class="fas fa-check-circle text-blue-600 mr-3 mt-1"></i>
                                <span>Perpanjang paket sebelum masa aktif habis untuk internet terus nyala</span>
                            </li>
                        </ul>
                    </div>

                    <!-- Action Buttons -->
                    <div class="grid grid-cols-1 md:grid-cols-2 gap-4">
                        <button onclick="window.print()" 
                                class="bg-white border-2 border-gray-300 text-gray-700 font-semibold py-3 px-6 rounded-lg hover:bg-gray-50 transition">
                            <i class="fas fa-print mr-2"></i>Cetak Invoice
                        </button>
                        <a href="/prepaid/portal/packages" 
                           class="bg-gradient-to-r from-blue-600 to-purple-600 text-white font-semibold py-3 px-6 rounded-lg text-center hover:shadow-lg transition">
                            <i class="fas fa-home mr-2"></i>Kembali ke Portal
                        </a>
                    </div>

                    <!-- Auto Redirect Notice -->
                    <div class="mt-6 text-center">
                        <label class="inline-flex items-center text-sm text-gray-600 cursor-pointer">
                            <input type="checkbox" id="autoRedirect" class="mr-2">
                            <span>Jangan redirect otomatis</span>
                        </label>
                        <p class="text-xs text-gray-500 mt-2">
                            Halaman akan redirect otomatis dalam 10 detik...
                        </p>
                    </div>
                </div>

                <!-- Success Footer -->
                <div class="bg-gradient-to-r from-green-50 to-emerald-50 px-8 py-6">
                    <div class="flex items-center justify-center text-center">
                        <div>
                            <p class="text-green-800 font-semibold mb-1">
                                <i class="fas fa-heart text-red-500 mr-2"></i>
                                Terima kasih telah menggunakan layanan kami!
                            </p>
                            <p class="text-green-600 text-sm">
                                Selamat menikmati internet cepat dan stabil
                            </p>
                        </div>
                    </div>
                </div>
            </div>

            <!-- Support Info -->
            <div class="mt-6 text-center text-sm text-gray-600">
                <p>
                    Butuh bantuan? 
                    <a href="#" class="text-blue-600 hover:underline">Hubungi Customer Service</a>
                </p>
            </div>
        </div>
    </div>

    <!-- Confetti Animation (Optional) -->
    <style>
        @keyframes confetti {
            0% { transform: translateY(-100vh) rotate(0deg); opacity: 1; }
            100% { transform: translateY(100vh) rotate(720deg); opacity: 0; }
        }
        .confetti {
            position: fixed;
            width: 10px;
            height: 10px;
            background: #f0f;
            animation: confetti 3s ease-in-out infinite;
        }
    </style>
</body>
</html>

