<!DOCTYPE html>
<html lang="id">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
</head>
<body class="bg-gray-50">
    <!-- Header -->
    <header class="bg-gradient-to-r from-blue-600 to-purple-600 text-white shadow-lg">
        <div class="container mx-auto px-4 py-4">
            <div class="flex justify-between items-center">
                <div>
                    <h1 class="text-2xl font-bold">Portal Prepaid</h1>
                    <p class="text-blue-100 text-sm">Selamat datang, <%= customerName %></p>
                </div>
                <a href="/prepaid/portal/logout" class="bg-white/20 hover:bg-white/30 px-4 py-2 rounded-lg transition">
                    <i class="fas fa-sign-out-alt mr-2"></i>Logout
                </a>
            </div>
        </div>
    </header>

    <div class="container mx-auto px-4 py-8">
        <!-- Active Subscription Alert -->
        <% if (activeSubscription) { %>
        <div class="bg-green-50 border-l-4 border-green-500 p-6 mb-8 rounded-lg shadow">
            <div class="flex items-start">
                <i class="fas fa-check-circle text-green-500 text-2xl mr-4 mt-1"></i>
                <div class="flex-1">
                    <h3 class="font-bold text-green-900 text-lg mb-2">Paket Aktif</h3>
                    <div class="grid grid-cols-1 md:grid-cols-3 gap-4 text-sm">
                        <div>
                            <p class="text-green-700 font-semibold">Paket:</p>
                            <p class="text-green-900"><%= activeSubscription.package_name %></p>
                        </div>
                        <div>
                            <p class="text-green-700 font-semibold">Speed:</p>
                            <p class="text-green-900"><%= activeSubscription.download_mbps %>/<%= activeSubscription.upload_mbps %> Mbps</p>
                        </div>
                        <div>
                            <p class="text-green-700 font-semibold">Berakhir:</p>
                            <p class="text-green-900">
                                <%= new Date(activeSubscription.expiry_date).toLocaleDateString('id-ID') %>
                                (<%= activeSubscription.days_remaining %> hari lagi)
                            </p>
                        </div>
                    </div>
                    <p class="text-green-600 text-xs mt-3">
                        <i class="fas fa-info-circle mr-1"></i>
                        Membeli paket baru akan menggantikan paket yang aktif saat ini
                    </p>
                </div>
            </div>
        </div>
        <% } else { %>
        <div class="bg-yellow-50 border-l-4 border-yellow-500 p-6 mb-8 rounded-lg shadow">
            <div class="flex items-start">
                <i class="fas fa-exclamation-triangle text-yellow-500 text-2xl mr-4 mt-1"></i>
                <div>
                    <h3 class="font-bold text-yellow-900 text-lg mb-2">Tidak Ada Paket Aktif</h3>
                    <p class="text-yellow-700">Silakan pilih paket di bawah untuk mengaktifkan internet Anda</p>
                </div>
            </div>
        </div>
        <% } %>

        <!-- Messages -->
        <% if (error) { %>
        <div class="bg-red-50 border-l-4 border-red-500 p-4 mb-6 rounded">
            <p class="text-red-700"><%= error %></p>
        </div>
        <% } %>

        <% if (success) { %>
        <div class="bg-green-50 border-l-4 border-green-500 p-4 mb-6 rounded">
            <p class="text-green-700"><%= success %></p>
        </div>
        <% } %>

        <!-- Packages Grid -->
        <h2 class="text-2xl font-bold text-gray-800 mb-6">
            <i class="fas fa-box mr-2 text-blue-600"></i>Pilih Paket Internet
        </h2>

        <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
            <% packages.forEach(function(pkg) { %>
            <div class="bg-white rounded-xl shadow-lg hover:shadow-2xl transition-all duration-300 transform hover:-translate-y-2 overflow-hidden border-2 border-gray-100">
                <!-- Package Header -->
                <div class="bg-gradient-to-r from-blue-500 to-purple-500 p-6 text-white text-center">
                    <div class="text-5xl mb-2">
                        <i class="fas fa-wifi"></i>
                    </div>
                    <h3 class="text-2xl font-bold mb-1"><%= pkg.name %></h3>
                    <p class="text-blue-100 text-sm"><%= pkg.package_type %></p>
                </div>

                <!-- Package Body -->
                <div class="p-6">
                    <!-- Speed -->
                    <div class="text-center mb-6">
                        <div class="text-4xl font-bold text-gray-800 mb-2">
                            <%= pkg.download_mbps %>
                            <span class="text-xl text-gray-500">Mbps</span>
                        </div>
                        <p class="text-sm text-gray-600">
                            Download <%= pkg.download_mbps %> Mbps / Upload <%= pkg.upload_mbps %> Mbps
                        </p>
                    </div>

                    <!-- Duration -->
                    <div class="bg-gray-50 rounded-lg p-4 mb-4">
                        <div class="flex items-center justify-between">
                            <span class="text-gray-600 text-sm">
                                <i class="far fa-clock mr-2"></i>Durasi
                            </span>
                            <span class="font-semibold text-gray-800">
                                <% if (pkg.package_type === 'daily') { %>
                                    <%= Math.floor(pkg.duration_hours / 24) %> Hari
                                <% } else if (pkg.package_type === 'weekly') { %>
                                    7 Hari
                                <% } else if (pkg.package_type === 'monthly') { %>
                                    30 Hari
                                <% } else { %>
                                    <%= pkg.duration_hours %> Jam
                                <% } %>
                            </span>
                        </div>
                    </div>

                    <!-- Data Limit (if any) -->
                    <% if (pkg.data_limit_gb) { %>
                    <div class="bg-blue-50 rounded-lg p-4 mb-4">
                        <div class="flex items-center justify-between">
                            <span class="text-blue-600 text-sm">
                                <i class="fas fa-database mr-2"></i>Kuota
                            </span>
                            <span class="font-semibold text-blue-800">
                                <%= pkg.data_limit_gb %> GB
                            </span>
                        </div>
                    </div>
                    <% } else { %>
                    <div class="bg-green-50 rounded-lg p-4 mb-4">
                        <div class="flex items-center justify-center">
                            <i class="fas fa-infinity text-green-600 mr-2"></i>
                            <span class="font-semibold text-green-800">Unlimited</span>
                        </div>
                    </div>
                    <% } %>

                    <!-- Price -->
                    <div class="border-t border-gray-200 pt-4 mb-4">
                        <div class="text-center">
                            <div class="text-3xl font-bold text-blue-600 mb-1">
                                Rp <%= pkg.price.toLocaleString('id-ID') %>
                            </div>
                            <p class="text-gray-500 text-sm">Harga sudah termasuk PPN</p>
                        </div>
                    </div>

                    <!-- Action Button -->
                    <form action="/prepaid/portal/packages/select" method="POST">
                        <input type="hidden" name="package_id" value="<%= pkg.id %>">
                        <button type="submit" 
                                class="w-full bg-gradient-to-r from-blue-600 to-purple-600 text-white font-bold py-3 px-6 rounded-lg hover:shadow-lg transform hover:scale-105 transition duration-200">
                            <i class="fas fa-shopping-cart mr-2"></i>Pilih Paket Ini
                        </button>
                    </form>
                </div>
            </div>
            <% }); %>
        </div>

        <% if (packages.length === 0) { %>
        <div class="bg-white rounded-lg shadow-lg p-12 text-center">
            <i class="fas fa-box-open text-6xl text-gray-300 mb-4"></i>
            <h3 class="text-xl font-bold text-gray-700 mb-2">Tidak Ada Paket Tersedia</h3>
            <p class="text-gray-500">Silakan hubungi admin untuk informasi lebih lanjut</p>
        </div>
        <% } %>

        <!-- Help Section -->
        <div class="mt-12 bg-white rounded-lg shadow-lg p-6">
            <h3 class="text-lg font-bold text-gray-800 mb-4">
                <i class="fas fa-question-circle text-blue-600 mr-2"></i>Bantuan
            </h3>
            <div class="grid grid-cols-1 md:grid-cols-3 gap-4 text-sm text-gray-600">
                <div class="flex items-start">
                    <i class="fas fa-check text-green-500 mr-3 mt-1"></i>
                    <div>
                        <p class="font-semibold text-gray-800">Pilih Paket</p>
                        <p>Pilih paket sesuai kebutuhan internet Anda</p>
                    </div>
                </div>
                <div class="flex items-start">
                    <i class="fas fa-check text-green-500 mr-3 mt-1"></i>
                    <div>
                        <p class="font-semibold text-gray-800">Pembayaran</p>
                        <p>Bayar melalui berbagai metode yang tersedia</p>
                    </div>
                </div>
                <div class="flex items-start">
                    <i class="fas fa-check text-green-500 mr-3 mt-1"></i>
                    <div>
                        <p class="font-semibold text-gray-800">Langsung Aktif</p>
                        <p>Internet otomatis aktif setelah pembayaran dikonfirmasi</p>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <!-- Footer -->
    <footer class="bg-gray-800 text-white mt-12 py-6">
        <div class="container mx-auto px-4 text-center">
            <p>&copy; 2025 Portal Prepaid. All rights reserved.</p>
        </div>
    </footer>
</body>
</html>

