<!DOCTYPE html>
<html lang="id">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %> - Login Portal</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <style>
        /* Ensure icons and text are visible */
        .fas, .fab, .far {
            display: inline-block !important;
            visibility: visible !important;
            opacity: 1 !important;
        }
        /* Fallback for icon visibility */
        i[class*="fa-"] {
            color: inherit !important;
        }
        /* Ensure text is always visible */
        label, p, span, h1, h2, h3, h4, h5, h6 {
            color: inherit !important;
        }
    </style>
</head>
<body class="bg-gradient-to-br from-blue-500 to-purple-600 min-h-screen flex items-center justify-center p-4">
    <div class="max-w-md w-full">
        <!-- Login Card -->
        <div class="bg-white rounded-2xl shadow-2xl overflow-hidden">
            <!-- Header -->
            <div class="bg-gradient-to-r from-blue-600 to-purple-600 p-8 text-center">
                <div class="w-20 h-20 bg-white rounded-full mx-auto mb-4 flex items-center justify-center">
                    <i class="fas fa-wifi text-4xl text-blue-600"></i>
                </div>
                <h1 class="text-2xl font-bold text-white mb-2">Portal Prepaid</h1>
                <p class="text-blue-100">Internet Cepat & Terpercaya</p>
            </div>

            <!-- Form -->
            <div class="p-8">
                <% if (error) { %>
                <div class="bg-red-50 border-l-4 border-red-500 p-4 mb-6 rounded">
                    <div class="flex items-center">
                        <i class="fas fa-exclamation-circle text-red-500 mr-3"></i>
                        <p class="text-red-700 text-sm"><%= error %></p>
                    </div>
                </div>
                <% } %>

                <% if (success) { %>
                <div class="bg-green-50 border-l-4 border-green-500 p-4 mb-6 rounded">
                    <div class="flex items-center">
                        <i class="fas fa-check-circle text-green-500 mr-3"></i>
                        <p class="text-green-700 text-sm"><%= success %></p>
                    </div>
                </div>
                <% } %>

                <form action="/prepaid/portal/login" method="POST" class="space-y-6">
                    <div>
                        <label for="portal_id" class="block text-sm font-medium text-gray-700 mb-2">
                            <i class="fas fa-user mr-2"></i>ID Portal
                        </label>
                        <input type="text" 
                               id="portal_id" 
                               name="portal_id" 
                               required
                               placeholder="Masukkan ID Portal Anda"
                               class="w-full px-4 py-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-blue-500 focus:border-transparent transition">
                        <p class="text-xs text-gray-500 mt-1">Contoh: CUST001234</p>
                    </div>

                    <div>
                        <label for="portal_pin" class="block text-sm font-medium text-gray-700 mb-2">
                            <i class="fas fa-lock mr-2"></i>PIN
                        </label>
                        <input type="password" 
                               id="portal_pin" 
                               name="portal_pin" 
                               required
                               placeholder="Masukkan PIN Anda"
                               class="w-full px-4 py-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-blue-500 focus:border-transparent transition">
                        <p class="text-xs text-gray-500 mt-1">PIN 4-6 digit yang Anda terima via WhatsApp</p>
                    </div>

                    <button type="submit" 
                            class="w-full bg-gradient-to-r from-blue-600 to-purple-600 text-white font-semibold py-3 px-6 rounded-lg hover:shadow-lg transform hover:scale-105 transition duration-200">
                        <i class="fas fa-sign-in-alt mr-2"></i>Masuk ke Portal
                    </button>
                </form>

                <!-- Help Text -->
                <div class="mt-6 pt-6 border-t border-gray-200">
                    <div class="bg-blue-50 rounded-lg p-4">
                        <h3 class="font-semibold text-blue-900 mb-2 flex items-center">
                            <i class="fas fa-info-circle mr-2"></i>Informasi
                        </h3>
                        <ul class="text-sm text-blue-800 space-y-2">
                            <li class="flex items-start">
                                <i class="fas fa-check text-blue-600 mr-2 mt-1"></i>
                                <span>ID Portal dan PIN dikirim via WhatsApp saat registrasi</span>
                            </li>
                            <li class="flex items-start">
                                <i class="fas fa-check text-blue-600 mr-2 mt-1"></i>
                                <span>Silakan hubungi admin jika lupa ID atau PIN</span>
                            </li>
                            <li class="flex items-start">
                                <i class="fas fa-check text-blue-600 mr-2 mt-1"></i>
                                <span>Akses portal ini untuk membeli paket internet</span>
                            </li>
                        </ul>
                    </div>
                </div>
            </div>

            <!-- Footer -->
            <div class="bg-gray-50 px-8 py-4 text-center text-sm text-gray-600">
                <p>&copy; 2025 Portal Prepaid. All rights reserved.</p>
            </div>
        </div>
    </div>
</body>
</html>

