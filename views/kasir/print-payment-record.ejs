<!DOCTYPE html>
<html lang="id">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Bukti Pembayaran #<%= payment.id %></title>
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
    <style>
        @page {
            size: A4;
            margin: 15mm;
        }
        
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }
        
        body {
            font-family: 'Arial', sans-serif;
            font-size: 11pt;
            line-height: 1.6;
            color: #333;
            background: #f5f5f5;
        }
        
        .container {
            max-width: 800px;
            margin: 20px auto;
            background: white;
            padding: 30px;
            box-shadow: 0 0 20px rgba(0,0,0,0.1);
        }
        
        .no-print {
            margin-bottom: 20px;
            text-align: right;
        }
        
        .btn {
            padding: 10px 20px;
            margin-left: 10px;
            border: none;
            border-radius: 6px;
            cursor: pointer;
            font-size: 11pt;
            font-weight: bold;
        }
        
        .btn-print {
            background: #2563eb;
            color: white;
        }
        
        .btn-back {
            background: #6b7280;
            color: white;
        }
        
        .header {
            text-align: center;
            margin-bottom: 40px;
            padding-bottom: 20px;
            border-bottom: 3px solid #2563eb;
        }
        
        .header h1 {
            font-size: 32pt;
            color: #2563eb;
            margin-bottom: 10px;
        }
        
        .header p {
            color: #666;
            font-size: 10pt;
        }
        
        .receipt-number {
            background: #f3f4f6;
            padding: 10px;
            border-radius: 8px;
            text-align: center;
            margin-bottom: 30px;
        }
        
        .receipt-number strong {
            font-size: 14pt;
            color: #1e40af;
        }
        
        .info-section {
            margin-bottom: 30px;
        }
        
        .section-title {
            font-size: 12pt;
            font-weight: bold;
            color: #2563eb;
            margin-bottom: 15px;
            padding-bottom: 8px;
            border-bottom: 2px solid #e5e7eb;
        }
        
        .info-grid {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 15px;
        }
        
        .info-item {
            padding: 12px;
            background: #f9fafb;
            border-radius: 6px;
            border-left: 4px solid #2563eb;
        }
        
        .info-label {
            font-size: 9pt;
            color: #666;
            margin-bottom: 5px;
        }
        
        .info-value {
            font-size: 11pt;
            font-weight: bold;
            color: #1e40af;
        }
        
        .payment-details {
            background: linear-gradient(135deg, #fef3c7 0%, #fde68a 100%);
            padding: 20px;
            border-radius: 12px;
            margin: 30px 0;
            border: 2px solid #f59e0b;
        }
        
        .payment-amount {
            text-align: center;
            margin: 20px 0;
        }
        
        .payment-amount-label {
            font-size: 11pt;
            color: #92400e;
            margin-bottom: 10px;
        }
        
        .payment-amount-value {
            font-size: 36pt;
            font-weight: bold;
            color: #78350f;
        }
        
        .status-badge {
            display: inline-block;
            padding: 12px 24px;
            background: #10b981;
            color: white;
            border-radius: 30px;
            font-size: 14pt;
            font-weight: bold;
            text-transform: uppercase;
            margin: 20px 0;
        }
        
        .footer {
            margin-top: 50px;
            padding-top: 20px;
            border-top: 2px solid #e5e7eb;
            text-align: center;
            font-size: 9pt;
            color: #888;
        }
        
        @media print {
            body {
                background: white;
            }
            
            .container {
                box-shadow: none;
                margin: 0;
                padding: 20px;
            }
            
            .no-print {
                display: none !important;
            }
        }
    </style>
</head>
<body>
    <div class="container">
        <!-- Print Buttons -->
        <div class="no-print">
            <button onclick="window.print()" class="btn btn-print">
                <i class="fas fa-print"></i> Print
            </button>
            <button onclick="window.location.href='/kasir/transactions'" class="btn btn-back">
                <i class="fas fa-arrow-left"></i> Kembali
            </button>
        </div>

        <!-- Header -->
        <div class="header">
            <h1>BUKTI PEMBAYARAN</h1>
            <p>Billing System - Internet Service Provider</p>
        </div>

        <!-- Receipt Number -->
        <div class="receipt-number">
            <strong>No. Receipt: #<%= payment.id %></strong><br>
            <span style="font-size: 10pt; color: #666;">
                <%= new Date(payment.payment_date || payment.created_at).toLocaleString('id-ID', { 
                    year: 'numeric', 
                    month: 'long', 
                    day: 'numeric',
                    hour: '2-digit',
                    minute: '2-digit'
                }) %>
            </span>
        </div>

        <!-- Customer Info -->
        <div class="info-section">
            <div class="section-title">
                <i class="fas fa-user"></i> Data Pelanggan
            </div>
            <div class="info-grid">
                <div class="info-item">
                    <div class="info-label">Kode Pelanggan</div>
                    <div class="info-value"><%= payment.customer_code || '-' %></div>
                </div>
                <div class="info-item">
                    <div class="info-label">Nama</div>
                    <div class="info-value"><%= payment.customer_name || '-' %></div>
                </div>
                <div class="info-item">
                    <div class="info-label">Telepon</div>
                    <div class="info-value"><%= payment.customer_phone || '-' %></div>
                </div>
                <div class="info-item">
                    <div class="info-label">Paket</div>
                    <div class="info-value"><%= payment.package_name || '-' %></div>
                </div>
            </div>
            <% if (payment.address) { %>
            <div class="info-item" style="margin-top: 15px;">
                <div class="info-label">Alamat</div>
                <div class="info-value" style="font-size: 10pt;"><%= payment.address %></div>
            </div>
            <% } %>
        </div>

        <!-- Invoice Info -->
        <div class="info-section">
            <div class="section-title">
                <i class="fas fa-file-invoice"></i> Detail Tagihan
            </div>
            <div class="info-grid">
                <div class="info-item">
                    <div class="info-label">No. Invoice</div>
                    <div class="info-value"><%= payment.invoice_number || '-' %></div>
                </div>
                <div class="info-item">
                    <div class="info-label">Periode</div>
                    <div class="info-value"><%= payment.period || '-' %></div>
                </div>
                <div class="info-item">
                    <div class="info-label">Total Tagihan</div>
                    <div class="info-value">Rp <%= (payment.invoice_total || 0).toLocaleString('id-ID') %></div>
                </div>
            </div>
        </div>

        <!-- Payment Details -->
        <div class="payment-details">
            <div class="payment-amount">
                <div class="payment-amount-label">JUMLAH PEMBAYARAN</div>
                <div class="payment-amount-value">Rp <%= (payment.amount || 0).toLocaleString('id-ID') %></div>
            </div>
            
            <div class="info-grid">
                <div class="info-item" style="background: white;">
                    <div class="info-label">Metode Pembayaran</div>
                    <div class="info-value"><%= payment.payment_method ? payment.payment_method.toUpperCase() : 'TUNAI' %></div>
                </div>
                <div class="info-item" style="background: white;">
                    <div class="info-label">Referensi</div>
                    <div class="info-value"><%= payment.reference_number || '-' %></div>
                </div>
            </div>
            
            <% if (payment.notes) { %>
            <div class="info-item" style="background: white; margin-top: 15px;">
                <div class="info-label">Catatan</div>
                <div class="info-value" style="font-size: 10pt;"><%= payment.notes %></div>
            </div>
            <% } %>
        </div>

        <!-- Status -->
        <div style="text-align: center;">
            <div class="status-badge">
                <i class="fas fa-check-circle"></i> PEMBAYARAN BERHASIL
            </div>
        </div>

        <!-- Kasir Info -->
        <div class="info-section">
            <div class="info-item">
                <div class="info-label">Diproses Oleh</div>
                <div class="info-value"><%= payment.kasir_name || 'System' %></div>
            </div>
        </div>

        <!-- Footer -->
        <div class="footer">
            <p><strong>Terima kasih atas pembayaran Anda!</strong></p>
            <p>Simpan bukti pembayaran ini sebagai dokumentasi.</p>
            <p style="margin-top: 15px;">
                Dokumen ini dicetak pada <%= new Date().toLocaleString('id-ID') %>
            </p>
        </div>
    </div>
</body>
</html>



