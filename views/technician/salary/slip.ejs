<!DOCTYPE html>
<html lang="id">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Slip Gaji - <%= payment.technician_name %>
    </title>
    <!-- Use Tailwind for easy styling -->
    <script src="https://cdn.tailwindcss.com"></script>
    <style>
        @media print {
            @page {
                size: A4;
                margin: 0;
            }

            body {
                -webkit-print-color-adjust: exact !important;
                print-color-adjust: exact !important;
                margin: 0;
                padding: 0;
                background-color: white !important;
            }

            .no-print {
                display: none !important;
            }

            /* Ensure container fills the page or centers correctly */
            .slip-container {
                width: 100% !important;
                max-width: none !important;
                box-shadow: none !important;
                border: none !important;
                border-radius: 0 !important;
                margin: 0 !important;
            }
        }

        .slip-bg {
            background-image: radial-gradient(#f3f4f6 1px, transparent 1px);
            background-size: 20px 20px;
        }
    </style>
</head>

<body class="bg-gray-100 min-h-screen flex flex-col items-center justify-center p-4">

    <!-- Print Button -->
    <div class="fixed top-4 right-4 no-print space-x-2">
        <button onclick="window.print()"
            class="bg-blue-600 text-white px-4 py-2 rounded shadow hover:bg-blue-700 flex items-center gap-2">
            <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
                    d="M17 17h2a2 2 0 002-2v-4a2 2 0 00-2-2H5a2 2 0 00-2 2v4a2 2 0 002 2h2m2 4h6a2 2 0 002-2v-4a2 2 0 00-2-2H9a2 2 0 00-2 2v4a2 2 0 002 2zm8-12V5a2 2 0 00-2-2H9a2 2 0 00-2 2v4h10z">
                </path>
            </svg>
            Cetak
        </button>
        <button onclick="window.close()" class="bg-gray-600 text-white px-4 py-2 rounded shadow hover:bg-gray-700">
            Tutup
        </button>
    </div>

    <!-- Slip Container -->
    <div
        class="slip-container bg-white w-full max-w-[210mm] shadow-xl rounded-none md:rounded-lg overflow-hidden border border-gray-200 mx-auto">

        <!-- Header -->
        <div class="bg-blue-900 text-white p-6">
            <div class="flex justify-between items-start">
                <div>
                    <h1 class="text-2xl font-bold tracking-wide uppercase">Slip Gaji</h1>
                    <p class="text-blue-200 text-sm mt-1">Periode: <%= new Date(payment.year, payment.month -
                            1).toLocaleString('id-ID', { month: 'long' , year: 'numeric' }) %>
                    </p>
                </div>
                <div class="text-right">
                    <h2 class="font-bold text-lg">Billing System</h2>
                    <p class="text-xs text-blue-200">Technician Payroll</p>
                </div>
            </div>
        </div>

        <!-- Content -->
        <div class="p-8">

            <!-- Employee Info -->
            <div class="flex justify-between border-b border-gray-100 pb-6 mb-6">
                <div>
                    <p class="text-xs text-gray-500 uppercase tracking-wider font-semibold">Nama Teknisi</p>
                    <h3 class="text-lg font-bold text-gray-800">
                        <%= payment.technician_name %>
                    </h3>
                    <p class="text-sm text-gray-600">
                        <%= payment.technician_phone || '-' %>
                    </p>
                </div>
                <div class="text-right">
                    <p class="text-xs text-gray-500 uppercase tracking-wider font-semibold">Tanggal Cetak</p>
                    <p class="text-sm text-gray-800 font-medium">
                        <%= new Date(payment.created_at).toLocaleDateString('id-ID', { day: 'numeric' , month: 'long' ,
                            year: 'numeric' }) %>
                    </p>
                    <p class="text-xs text-gray-500 mt-1">ID Pembayaran: #<%= String(payment.id).padStart(6, '0' ) %>
                    </p>
                </div>
            </div>

            <!-- Earnings -->
            <div class="mb-6">
                <h4 class="text-sm font-semibold text-gray-900 border-b border-gray-200 pb-2 mb-4 uppercase">Rincian
                    Pendapatan</h4>

                <div class="space-y-3">
                    <div class="flex justify-between items-center text-sm">
                        <span class="text-gray-600">Gaji Pokok / Kehadiran (<%= payment.total_attendance_days %>
                                hari)</span>
                        <span class="font-medium text-gray-900">Rp <%=
                                Number(payment.base_salary).toLocaleString('id-ID') %></span>
                    </div>
                    <div class="flex justify-between items-center text-sm">
                        <span class="text-gray-600">Komisi Pekerjaan (<%= payment.total_jobs %> Job)</span>
                        <span class="font-medium text-gray-900 text-xs italic text-gray-400">(Termasuk dalam
                            bonus/gaji)</span>
                    </div>
                    <div class="flex justify-between items-center text-sm">
                        <span class="text-gray-600">Bonus & Tunjangan Lain</span>
                        <span class="font-medium text-gray-900">Rp <%=
                                Number(payment.bonus_amount).toLocaleString('id-ID') %></span>
                    </div>
                </div>
            </div>

            <!-- Deductions -->
            <% if (payment.deduction_amount> 0) { %>
                <div class="mb-6">
                    <h4 class="text-sm font-semibold text-red-700 border-b border-red-100 pb-2 mb-4 uppercase">Potongan
                    </h4>
                    <div class="space-y-3">
                        <div class="flex justify-between items-center text-sm">
                            <span class="text-gray-600">Potongan (Kasbon/Lainnya)</span>
                            <span class="font-medium text-red-600">- Rp <%=
                                    Number(payment.deduction_amount).toLocaleString('id-ID') %></span>
                        </div>
                    </div>
                </div>
                <% } %>

                    <!-- Total -->
                    <div class="bg-gray-50 rounded-lg p-4 border border-gray-200 mb-6">
                        <div class="flex justify-between items-center">
                            <div>
                                <p class="text-sm font-bold text-gray-700 uppercase">Total Diterima</p>
                                <p class="text-xs text-gray-500">Take Home Pay</p>
                            </div>
                            <p class="text-2xl font-bold text-blue-800">Rp <%=
                                    Number(payment.final_amount).toLocaleString('id-ID') %>
                            </p>
                        </div>
                    </div>

                    <!-- Footer / Notes -->
                    <div class="grid grid-cols-2 gap-8 mt-12">
                        <div>
                            <p class="text-xs text-gray-500 mb-1">Catatan:</p>
                            <p class="text-xs text-gray-600 italic bg-yellow-50 p-2 rounded border border-yellow-100">
                                <%= payment.notes || 'Tidak ada catatan khusus.' %>
                            </p>
                        </div>
                        <div class="text-center">
                            <p class="text-xs font-semibold text-gray-500 mb-12">Disetujui Oleh,</p>
                            <p
                                class="text-sm font-bold text-gray-900 border-b border-gray-300 pb-1 inline-block min-w-[150px]">
                                <%= payment.admin_name || 'Admin' %>
                            </p>
                        </div>
                    </div>
        </div>

        <!-- Bottom Bar -->
        <div class="bg-gray-50 px-8 py-4 border-t border-gray-200 flex justify-between items-center">
            <p class="text-xs text-gray-400">Dicetak otomatis oleh sistem.</p>
            <p class="text-xs text-gray-400">Billing System &copy; <%= new Date().getFullYear() %>
            </p>
        </div>
    </div>

</body>

</html>