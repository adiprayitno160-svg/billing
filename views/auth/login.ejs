<!DOCTYPE html>
<html lang="id">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>
        <%= title %>
    </title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" rel="stylesheet">
    <script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>
    <style>
        @import url('https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&display=swap');

        body {
            font-family: 'Inter', sans-serif;
            background-color: #0f172a;
            overflow: hidden;
            height: 100vh;
        }

        /* Animated Background */
        .bg-animation {
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            z-index: 0;
        }

        .bg-gradient-mesh {
            position: absolute;
            width: 150%;
            height: 150%;
            top: -25%;
            left: -25%;
            background: radial-gradient(circle at 50% 50%, rgba(37, 99, 235, 0.15) 0%, transparent 50%),
                radial-gradient(circle at 0% 0%, rgba(147, 51, 234, 0.15) 0%, transparent 50%),
                radial-gradient(circle at 100% 100%, rgba(6, 182, 212, 0.15) 0%, transparent 50%);
            animation: moveGradient 20s ease-in-out infinite alternate;
            filter: blur(60px);
        }

        @keyframes moveGradient {
            0% {
                transform: translate(0, 0) rotate(0deg);
            }

            100% {
                transform: translate(-10%, -10%) rotate(5deg);
            }
        }

        /* Glassmorphism Card */
        .login-card {
            background: rgba(255, 255, 255, 0.03);
            backdrop-filter: blur(20px);
            -webkit-backdrop-filter: blur(20px);
            border: 1px solid rgba(255, 255, 255, 0.1);
            box-shadow: 0 25px 50px -12px rgba(0, 0, 0, 0.5);
            transition: all 0.3s ease;
        }

        .login-card:hover {
            border-color: rgba(255, 255, 255, 0.2);
            box-shadow: 0 30px 60px -15px rgba(0, 0, 0, 0.6);
        }

        /* Input Styles */
        .input-group {
            position: relative;
        }

        .input-field {
            background: rgba(15, 23, 42, 0.6);
            border: 1px solid rgba(255, 255, 255, 0.1);
            color: #e2e8f0;
            transition: all 0.3s ease;
        }

        .input-field:focus {
            background: rgba(15, 23, 42, 0.8);
            border-color: #3b82f6;
            box-shadow: 0 0 0 2px rgba(59, 130, 246, 0.2);
        }

        /* Floating Label Effect */
        .floating-label {
            position: absolute;
            left: 3rem;
            top: 50%;
            transform: translateY(-50%);
            color: #94a3b8;
            pointer-events: none;
            transition: all 0.2s ease;
            font-size: 0.95rem;
        }

        .input-field:focus~.floating-label,
        .input-field:not(:placeholder-shown)~.floating-label {
            top: 0;
            transform: translateY(-50%) scale(0.85);
            background: #0f172a;
            padding: 0 0.5rem;
            left: 0.75rem;
            color: #60a5fa;
            font-weight: 500;
            border-radius: 4px;
        }

        .input-icon {
            position: absolute;
            left: 1rem;
            top: 50%;
            transform: translateY(-50%);
            color: #64748b;
            transition: color 0.3s ease;
        }

        .input-field:focus~.input-icon {
            color: #3b82f6;
        }

        /* Button Glow */
        .btn-glow {
            box-shadow: 0 0 15px rgba(37, 99, 235, 0.3);
            transition: all 0.3s ease;
        }

        .btn-glow:hover {
            box-shadow: 0 0 25px rgba(37, 99, 235, 0.5);
            transform: translateY(-2px);
        }

        .btn-glow:active {
            transform: translateY(0);
        }
    </style>
</head>

<body class="flex items-center justify-center p-4">
    <!-- Animated Background -->
    <div class="bg-animation">
        <div class="bg-gradient-mesh"></div>
        <!-- Particles (CSS only representation) -->
        <div
            class="absolute inset-0 bg-[url('data:image/svg+xml;base64,PHN2ZyB3aWR0aD0iMjAiIGhlaWdodD0iMjAiIHhtbG5zPSJodHRwOi8vd3d3LnczLm9yZy8yMDAwL3N2ZyI+PGNpcmNsZSBjeD0iMSIgY3k9IjEiIHI9IjEiIGZpbGw9InJnYmEoMjU1LDI1NSwyNTUsMC4wNSkiLz48L3N2Zz4=')] opacity-20">
        </div>
    </div>

    <div class="relative z-10 w-full max-w-sm">
        <!-- Main Card -->
        <div class="login-card rounded-3xl p-8 relative overflow-hidden">
            <!-- Top Decoration -->
            <div class="absolute top-0 left-0 w-full h-1 bg-gradient-to-r from-blue-500 via-purple-500 to-pink-500">
            </div>

            <!-- Header -->
            <div class="text-center mb-10">
                <div class="relative inline-block mb-4 group">
                    <div
                        class="absolute inset-0 bg-blue-500 rounded-full blur-lg opacity-40 group-hover:opacity-60 transition duration-500">
                    </div>
                    <div
                        class="relative w-16 h-16 bg-gradient-to-br from-slate-800 to-slate-900 rounded-2xl flex items-center justify-center border border-slate-700 shadow-xl group-hover:scale-110 transition duration-300">
                        <i class="fas fa-bolt text-3xl text-blue-400"></i>
                    </div>
                </div>
                <h1 class="text-2xl font-bold text-white tracking-tight mb-1">Billing System</h1>
                <p class="text-slate-400 text-sm">Masuk ke dashboard panel</p>
            </div>

            <!-- Login Form -->
            <form action="/login" method="POST" class="space-y-6" id="loginForm">
                <!-- Username -->
                <div class="input-group">
                    <input type="text" id="username" name="username" required
                        class="input-field w-full pl-12 pr-4 py-3.5 rounded-xl outline-none text-sm placeholder-transparent"
                        placeholder="Username" autocomplete="off"
                        value="<%= typeof username !== 'undefined' ? username : '' %>">
                    <i class="fas fa-user input-icon text-lg"></i>
                    <label for="username" class="floating-label">Username</label>
                </div>

                <!-- Password -->
                <div class="input-group">
                    <input type="password" id="password" name="password" required
                        class="input-field w-full pl-12 pr-12 py-3.5 rounded-xl outline-none text-sm placeholder-transparent"
                        placeholder="Password">
                    <i class="fas fa-lock input-icon text-lg"></i>
                    <label for="password" class="floating-label">Password</label>

                    <button type="button" onclick="togglePassword()"
                        class="absolute right-4 top-1/2 transform -translate-y-1/2 text-slate-500 hover:text-blue-400 transition-colors focus:outline-none z-10">
                        <i id="passwordIcon" class="fas fa-eye"></i>
                    </button>
                </div>

                <!-- Submit Button -->
                <button type="submit" id="submitBtn"
                    class="btn-glow w-full bg-gradient-to-r from-blue-600 to-indigo-600 hover:from-blue-500 hover:to-indigo-500 text-white font-semibold py-3.5 rounded-xl transition-all duration-300 flex items-center justify-center gap-2 group">
                    <span class="group-hover:translate-x-1 transition-transform">Masuk Sekarang</span>
                    <i class="fas fa-arrow-right text-sm opacity-70 group-hover:translate-x-1 transition-transform"></i>
                </button>
            </form>

            <!-- Footer -->
            <div class="mt-8 pt-6 border-t border-slate-700/50 text-center">
                <p class="text-xs text-slate-500">&copy; 2026 Billing System. Secure Access.</p>
            </div>
        </div>
    </div>

    <!-- Hidden Flash Data for SweetAlert -->
    <div id="flash-error"
        data-msg="<%= (typeof flash !=='undefined' && flash.error && flash.error.length > 0) ? flash.error[0] : '' %>">
    </div>
    <div id="flash-success"
        data-msg="<%= (typeof flash !=='undefined' && flash.success && flash.success.length > 0) ? flash.success[0] : '' %>">
    </div>

    <script>
        // SweetAlert Configuration
        const Toast = Swal.mixin({
            toast: true,
            position: 'top-end',
            showConfirmButton: false,
            timer: 3000,
            timerProgressBar: true,
            didOpen: (toast) => {
                toast.addEventListener('mouseenter', Swal.stopTimer)
                toast.addEventListener('mouseleave', Swal.resumeTimer)
            }
        });

        // Toggle Password
        function togglePassword() {
            const passwordInput = document.getElementById('password');
            const passwordIcon = document.getElementById('passwordIcon');

            if (passwordInput.type === 'password') {
                passwordInput.type = 'text';
                passwordIcon.classList.remove('fa-eye');
                passwordIcon.classList.add('fa-eye-slash');
            } else {
                passwordInput.type = 'password';
                passwordIcon.classList.remove('fa-eye-slash');
                passwordIcon.classList.add('fa-eye');
            }
        }

        // Initialize Styles & Events
        document.addEventListener('DOMContentLoaded', function () {
            const usernameInput = document.getElementById('username');
            const flashError = document.getElementById('flash-error').dataset.msg;
            const flashSuccess = document.getElementById('flash-success').dataset.msg;
            const loginForm = document.getElementById('loginForm');
            const submitBtn = document.getElementById('submitBtn');

            // Focus username
            usernameInput.focus();

            // Check URL parameters for additional messages
            const urlParams = new URLSearchParams(window.location.search);
            const urlError = urlParams.get('error');
            const urlSuccess = urlParams.get('success');

            // Display Notifications
            if (flashError || urlError) {
                Swal.fire({
                    icon: 'error',
                    title: 'Login Gagal',
                    text: flashError || urlError,
                    background: '#1e293b',
                    color: '#fff',
                    confirmButtonColor: '#3b82f6'
                });
                // Remove query param
                if (urlError) window.history.replaceState({}, document.title, window.location.pathname);
            }

            if (flashSuccess || urlSuccess) {
                Toast.fire({
                    icon: 'success',
                    title: flashSuccess || urlSuccess
                });
                // Remove query param
                if (urlSuccess) window.history.replaceState({}, document.title, window.location.pathname);
            }

            // Timeout Handling
            if (urlParams.get('timeout') === '1') {
                Swal.fire({
                    icon: 'warning',
                    title: 'Sesi Berakhir',
                    text: 'Silakan login kembali untuk melanjutkan.',
                    background: '#1e293b',
                    color: '#fff',
                    confirmButtonColor: '#eab308'
                });
            }

            // Form Submit Animation
            loginForm.addEventListener('submit', function (e) {
                const username = document.getElementById('username').value;
                const password = document.getElementById('password').value;

                if (!username || !password) {
                    e.preventDefault();
                    Swal.fire({
                        icon: 'warning',
                        title: 'Perhatian',
                        text: 'Harap isi username dan password',
                        background: '#1e293b',
                        color: '#fff',
                        confirmButtonColor: '#3b82f6'
                    });
                    return;
                }

                // Show loading state
                submitBtn.disabled = true;
                submitBtn.innerHTML = `
                    <svg class="animate-spin -ml-1 mr-3 h-5 w-5 text-white" xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24">
                        <circle class="opacity-25" cx="12" cy="12" r="10" stroke="currentColor" stroke-width="4"></circle>
                        <path class="opacity-75" fill="currentColor" d="M4 12a8 8 0 018-8V0C5.373 0 0 5.373 0 12h4zm2 5.291A7.962 7.962 0 014 12H0c0 3.042 1.135 5.824 3 7.938l3-2.647z"></path>
                    </svg>
                    Memproses...
                `;
                submitBtn.classList.add('opacity-75', 'cursor-not-allowed');
            });
        });
    </script>
</body>

</html>