============================================================
🔍 TEST EXCEL IMPORT - Debug Logging
============================================================

Perbaikan sudah di-push ke GitHub.
Sekarang update server dan test import.

============================================================
LANGKAH UPDATE SERVER:
============================================================

cd /opt/billing && \
git pull origin main && \
npm install && \
npm run build && \
npm install --production && \
pm2 restart billing-app --update-env


============================================================
TEST IMPORT EXCEL:
============================================================

1. Download template dari: http://your-server:3000/customers/list
2. Isi dengan data test, contoh:
   
   Kolom 1: Nama       | Kolom 2: Telepon | Kolom 3: Alamat
   --------------------+------------------+--------------------
   Budi Santoso        | 081234567890     | Jl. Merdeka No. 123
   Siti Aminah         | 082345678901     | Jl. Kenanga No. 45

3. Upload file Excel
4. Cek logs di terminal untuk debug info:

   pm2 logs billing-app --lines 100

   Cari log:
   🔍 Available columns: [...]
   🔍 Normalized map: {...}
   🔍 Looking for: [...]
   ✅ Exact match found: ...
   ❌ No match found for: ...


============================================================
KEMUNGKINAN MASALAH:
============================================================

1. HEADER EXCEL TIDAK COCOK
   - Cek log: "Available columns" vs "Looking for"
   - Pastikan header Excel sama dengan template
   
2. KARAKTER KHUSUS DI HEADER
   - Header mungkin punya NBSP, karakter tersembunyi
   - Normalize seharusnya sudah handle ini
   
3. KOLOM KOSONG
   - Excel mungkin punya kolom kosong/hidden
   - Cek semua kolom yang terdeteksi

============================================================
SOLUSI JIKA MASIH GAGAL:
============================================================

Print semua informasi debug:

pm2 logs billing-app | grep -A5 -B5 "Available columns"

Copy hasil log dan kirim ke developer untuk analisis.

============================================================

